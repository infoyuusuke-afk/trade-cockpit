# trade-cockpit STATUS

最終更新: 2026-09-15（Claude・P1続き：決算発表後の監視候補(earnings_watch.py)を実装、実行ワークフローはpush待ち）
役割分担確定：ChatGPT=戦略の壁打ちのみ（リポジトリは変更しない）／Claude=実コーディング担当
運用体制: ChatGPT（戦略・相場観）＋ Claude/Claude Code（コーディング・診断）＋ Genspark（必要時のみ、現在未課金）

新しいスレッド・チャットを始めるときは、このファイルの内容をコピペするか
URL（https://raw.githubusercontent.com/infoyuusuke-afk/trade-cockpit/main/STATUS.md）
を貼るだけで、これまでの経緯を再説明せずに済みます。

---

## システム概要

- リポジトリ: infoyuusuke-afk/trade-cockpit（GitHub Pages: https://infoyuusuke-afk.github.io/trade-cockpit/）
- 構成: GitHub Actionsで自動実行するPythonパイプライン → JSONデータ生成 → 静的ダッシュボード(index.html)
- 対象銘柄: キオクシアホールディングス(285A)中心＋MS2 RSSで監視する100銘柄
- 証券会社: 楽天証券 マーケットスピード2 RSS

## 目指しているゴール

デイトレ（今日）／スイング（1週間）／長期（1か月）の3階層で、
仕手筋・機関・大口の初動の"足跡"を検知して勝てるシステムを構築する。
3階層は優先順位をつけず並行して育てる方針。

## 現状の3階層の実装状況

| 階層 | 状態 | 詳細 |
|---|---|---|
| デイトレ | 稼働中（要改善） | 100銘柄スキャンはLONGブレイクアウトのみ。SHORTはKioxia含む一部銘柄でMVP実弾化完了（scripts/short_candidates.py、LONGとは独立集計） |
| スイング(1週間) | MVP稼働開始 | swing_signal.py新規作成、investor_regime.json＋credit_supply.jsonで判定 |
| 長期(1か月) | MVP稼働開始 | long_signal.py新規作成、信用残高4週トレンド＋自社株買い＋セクター相関で判定 |

## 判明した重要な問題点（2026-09-14診断）

1. **デイトレのscoreが機能していない**：paper_trade_history.json（169件）を集計した結果、
   scoreは発動率・勝敗とほとんど相関なし。全体成績は勝率39.5%・平均R+0.03・PF1.17（n=43でまだ弱い）。
2. **SHORTが検知されているのに一度も実弾化されていない**：signal_scan.pyのanalyse_short()は
   SHORTセットアップを実際に検知している（直近80コミットで確認）が、実際に紙トレードを生成する
   update.pyのbuild_day_ifo_candidates()はLONGブレイクアウト専用の別ロジックでSHORT分岐が存在しない。
3. 診断に必要な生データ（rvol/ret20/atr_pct/swing_score等）がpaper_trade_history.jsonに
   残っていなかったため、morning_review.pyをパッチして今後の記録に含めるよう修正済み。

## 直近で完了した作業

- [x] リポジトリ構造の調査、既存ロジックの棚卸し
- [x] paper_trade_history.jsonの統計診断
- [x] SHORT未接続問題の原因特定
- [x] morning_review.pyパッチ（診断用の生データを記録に残す）
- [x] scripts/tag_horizons.py, swing_signal.py, long_signal.py 新規作成（実データで動作検証済み）
- [x] .github/workflows/swing-long-signals.yml 追加（平日15:40 JST自動実行）

## 追加実装（2026-09-14 第2弾）

- **SHORT実弾化を完了**：scripts/short_candidates.py（MVP・下降配列＋出来高＋信用需給で判定、最大5件）を新規作成し、
  scripts/morning_review.py にload_short_candidates()を追加して朝スナップショットにLONGと一緒に混在させるよう修正。
  audit_one()は元々side分岐に対応済みだったため、SHORTのみのバグだった（正確な原因）。
  実データで動作確認済み：LONG1件+SHORT3件が同じスナップショットに混在することを検証済み。
  statistics()もLONG/SHORTを合算せず側別に独立集計するよう変更。
- **信頼度ゲート**：scripts/reliability_report.py新規作成。resolved_n（発動して勝敗確定した件数）が
  30件未満の区分は勝率・PFを「試運転・検証中」として扱い、以降も95%信頼区間を併記する設計。
  現状：LONG resolved_n=43（勝率39.5%、95%CI 26.4-54.4%、まだ結論を出せる精度ではない）、SHORT resolved_n=0。
- **Jumping Point!! 株Tube連携の第一段階**：scripts/mention_tracker.py新規作成。
  チャンネルID UCgDa6dxD3jPElw6eCvL1T2g（毎週日曜20時更新、翌週注目銘柄ランキング形式）の
  YouTube公式RSS（認証不要）を定期ポーリングし、published_atとdetected_atの両方を記録することで
  未来情報の混入を防ぐ設計。タイトル/説明欄からの銘柄コード自動抽出は正規表現によるベストエフォートで、
  confirmed_tickersへの手動確認を前提とする（自動抽出をそのまま分析に使わないこと）。
- **監視TOP5の第1段階**：scripts/watch_top5.py新規作成。前日引け後時点の情報のみでTOP5を確定し、
  decision_asofを記録。株Tubeの紹介有無はスコアに一切使わず、後段の的中率検証用の参考フラグとしてのみ保持。
- ワークフロー変更：.github/workflows/update.ymlに short_candidates.py と reliability_report.py の
  実行ステップを追加（update.pyの直後、morning_review.pyの直前）。swing-long-signals.ymlにwatch_top5.pyを追加。
  新規 .github/workflows/mention-tracker.yml を追加（日曜18-22時は15分おき、平日は07:50に1回）。

## SNS/メディア全般の話題化銘柄監視（2026-09-14 第3弾・方針転換）

Jumping Point!! は一例に過ぎず、それに固執しない方針に変更。狙いは
「SNS・多メディア全般で仕手化・拡散されそうな銘柄」を横断監視し、
"次のキオクシア"を早期発見すること。

- **mention_tracker.pyを複数チャンネル対応に一般化**：WATCHED_CHANNELSリストに
  チャンネルを追加していく方式に変更（Jumping Pointはその1つ）。
- **board_buzz_ranking.py新規作成**：Yahoo!ファイナンスが公式に毎日発表している
  「掲示板投稿数ランキング」（全市場、認証不要、
  https://finance.yahoo.co.jp/stocks/ranking/bbs?market=all ）を取得。
  特定の配信者に依存しない、最も入手性の良い汎用SNS注目度シグナル。
  新規ランクイン・順位急上昇を観測用フラグとして記録（売買判断には使わない）。
  .github/workflows/board-buzz.yml で平日07:00 JSTに自動実行。
- **正直な制約**：board_buzz_ranking.pyの正規表現パーサーは実際のYahoo側HTML構造を
  完全な形では検証できておらず（開発環境からアクセス不可のため）、
  mention_tracker.pyのRSS方式より壊れやすい。導入後は必ず最初の数回、
  Actionsのログとboard_buzz_history.jsonの中身を目視確認すること。
  抽出0件が続く場合はHTML構造変更を疑う。

## デプロイ状況（2026-09-14 全て本番反映・稼働確認済み）

STATUS.md含む全14ファイルをGitHub Web UIから手動アップロードし、各ワークフローを手動実行して
以下を確認済み：
- signals_day.json / signals_swing.json / signals_long.json / watch_top5.json（スイング&ロング系）
- mentions.json（180件、12チャンネル分のRSS取得成功。Jumping Pointの本日9/14公開分も検知済み）
- board_buzz_history.json（Yahoo!ファイナンス掲示板投稿数ランキング、285Aが1位で取得成功）
- day_ifo_candidates_short.json / reliability_report.json（LONG/SHORT混在の朝スナップショット）

デプロイ中に見つけたバグ2件を修正済み：
1. swing-long-signals.yml / mention-tracker.yml / board-buzz.yml の3ワークフローで、
   `git diff --quiet`が新規ファイル（リポジトリに一度も存在しないファイル）を検知できず、
   コミット処理がスキップされていた。`git add`してから`git diff --cached --quiet`で判定する方式に修正。
2. board_buzz_ranking.pyの初版は正規表現でHTMLをパースする方式だったが、実際のYahoo側HTML構造と
   合わず0件抽出だった。pandas.read_html()で`<table>`要素を直接読む方式に変更し、対応する
   `pip install pandas lxml`ステップもboard-buzz.ymlに追加して解決。

## Stage②着手：手法比較の第一弾結果（2026-09-14）

scripts/stage2_backtest.py を新規作成。キオクシア(285A)の実5分足データ（直近25営業日、
kioxia_5m_calendar.json）を使い、OR5/OR15を使った3つのエントリー手法を同一の
エントリー閾値・利確幅・損切り幅で公平に比較した（OR15を最初から正式トリガーだと
決めつけない、というユーザー方針に沿う）。

| 手法 | 発動数(n) | 勝率 | 平均R | PF | 最大DD(R) |
|---|---|---|---|---|---|
| A: OR5早期参入 | 13 | 61.5% | +0.49 | 2.21 | 2.1 |
| B: OR5監視→OR15参加 | 11 | 45.5% | +0.09 | 1.15 | 3.8 |
| C: OR5参入・OR15利確 | 13 | 53.8% | +0.37 | 2.13 | 2.66 |

候補母数（発動有無に関わらず）は全手法とも25日（キオクシアの全営業日）。

**重要な制約（このまま実戦適用しないこと）**:
- 全手法n<30のため、ユーザー指定のゲート通り「試運転・検証中」表示。30件に届いても
  それだけで優位性確立とは表示しない方針を維持する。
- kioxia_5m_calendar.jsonのpathは5分足終値時点のリターン系列で、ザラ場中のヒゲ
  （高値・安値）を含まない。そのため損切りが実際より遅れる（成績が良く出過ぎる）
  方向のバイアスが構造的にある。ティック・板データでの精緻化が必要。
- 100銘柄ユニバース側は同水準の5分足履歴を持たないため、今回はキオクシア1銘柄のみ。
  他銘柄に広げるにはkioxia_calendar.py相当の5分足収集をwatch_top5銘柄にも拡張する
  必要がある（未着手）。
- エントリー閾値・利確幅・損切り幅（0%/1.5%/1.0%）は暫定の共通値。最適化はしていない
  （3手法で同じ値を使うことが比較の公平性の要件のため）。
- SHORT側の実売可能性チェック（楽天証券での貸借銘柄区分・売禁・在庫・逆日歩）は
  まだデータソースを持っていない。東証が毎日公表している「日々公表銘柄」等の
  公開データを新たに収集する必要がある（未着手、次の候補タスク）。

## Stage②基盤拡張（2026-09-14 第4弾・未デプロイ）

ユーザー指示「両方」を受け、以下2つを新規作成（まだGitHubにはアップロードしていない、
次回アップロード時に反映予定）：

- **scripts/multi_ticker_5m_calendar.py**：kioxia_calendar.pyと同じyfinance 5分足取得方式を
  watch_top5.json記載の銘柄＋キオクシアに拡張。従来のkioxia_5m_calendar.jsonが終値だけの
  近似だった反省を踏まえ、今回は5分足の高値・安値（ヒゲ）もそのまま保持する設計にした
  （data/5m_calendars/<code>.json）。yfinanceの60日制限があるため過去に遡っては取れず、
  導入後から蓄積が始まる。swing-long-signals.ymlに実行ステップを追加済み（コード未反映）。
- **scripts/margin_caution_check.py**：東証公式「日々公表銘柄」ページ
  （https://www.jpx.co.jp/markets/equities/margin-daily/）を取得し、
  ※付き（規制中）銘柄をmargin_caution.jsonに記録。short_candidates.pyに組み込み、
  規制中銘柄はSHORT候補から自動除外するようにした。ただし楽天証券固有の
  一般信用在庫切れ・逆日歩（品貸料）はこのデータでは分からず、別途要確認のまま。
  update.ymlに実行ステップを追加済み（コード未反映）。

**正直な制約**：どちらも開発環境からは実地テストできていない（サンドボックスから
Yahoo Finance/JPXへ接続不可）。パーサー部分は前回board_buzz_ranking.pyで学んだ
pandas.read_html方式や、kioxia_calendar.pyで実績のあるyfinance呼び出しパターンを
踏襲しているが、初回実行時は必ずActionsログとdata/5m_calendars/・margin_caution.jsonの
中身を確認すること。

## 重要な発見：VWAP未満エントリーvsVWAP以上エントリー（2026-09-14）

「勝ったREBOUNDと負けたREBOUNDで何が違うか」の検証依頼を受け、scripts/vwap_entry_analysis.py
を新規作成し、既存のpaper_trade_history.json（169件）で実際に検証した。

| 区分 | n | 勝率 | 平均R | PF |
|---|---|---|---|---|
| entryがVWAP(引け時点)未満（押し目型） | 21 | 61.9% | +0.333 | 4.28 |
| entryがVWAP(引け時点)以上（伸び追随型） | 22 | 18.2% | -0.253 | 0.08 |

システム全体の成績（勝率39.5%、PF1.17）は、実質的に「VWAP以上で追随エントリーした
負け組」が足を引っ張り、「VWAP未満で入れた勝ち組」がそれを相殺していただけだった
可能性が高い。ユーザーの「押し目を作ってから再度VWAP上に戻った方が勝率が高いはず」
という仮説と方向が一致する。

**正直な制約**：
- paper_trade_history.jsonには値動きの経路（実際に押し目を作ったかどうか）は
  記録されておらず、「entryがその日のVWAPより高いか低いか」という代理指標での検証。
  本来の「押し目からの再上昇」そのものを検証したわけではない。
- 両区分ともn<30のため「試運転・検証中」のまま。ただし差がかなり大きい（PF4.28 vs 0.08）
  ため、次の一手として優先度が高いと考えられる。
- update.ymlに実行ステップを追加済み（コード未反映、次回アップロード時に反映）。

**次に検討すべきこと**：build_day_ifo_candidates()やPine Scriptインジケーターの
エントリー条件に「VWAP以下でのみエントリー」フィルターを追加する案。ただしこれも
n<30の段階での示唆に過ぎないため、慎重に扱うこと。

## Stage②再検証：真のヒゲ付きデータでn=184に到達（2026-09-14）

ユーザーがSwing & Long Horizon Signalsを手動実行し、multi_ticker_5m_calendar.pyが
実際にwatch_top5経由の6銘柄（278A, 285A, 3132, 4667, 485A, 9984）×約60日分の
真のOHLC（ヒゲ付き）5分足データを取得できた。これを使い scripts/stage2_backtest_v2.py
を新規作成し、Stage②の3手法比較を、v1（キオクシア1銘柄・close近似・n=13）より
大幅に大きく信頼できる形で再実行した。

| 手法 | 候補日数 | 発動n | 勝率 | 平均R | PF | 最大DD |
|---|---|---|---|---|---|---|
| A: OR5早期参入 | 359 | 184 | 51.1% | +0.222 | 1.44 | 10.0 |
| B: OR5監視→OR15参加 | 359 | 135 | 47.4% | +0.127 | 1.24 | 10.85 |
| C: OR5参入・OR15利確 | 359 | 194 | 41.8% | +0.114 | 1.24 | 16.67 |

**Aが全指標で最良**、n=184とn>=30ゲートを初めて超えた。v1（n=13）と同じ結論
（OR15待ちは不利）を、より大きなサンプルと真のヒゲデータで再確認できた。

**開発中に見つけたバグ1件（修正済み）**：初版のC（OR5参入・OR15利確）は、
OR15水準で機械的に利確する際に、その手前で損切りに触れていないかのチェックを
していなかった（現実の注文なら損切り注文は生きたままのはず）。これにより
最大DDが21.42と過大に出ていた。損切りチェックを先に行うよう修正し16.67に是正。

**正直な制約（このままn>=30を"優位性確立"と受け取らないこと）**：
- data/5m_calendars/の銘柄群は「その時点でwatch_top5に載っていた」銘柄の
  寄せ集めで、常に同じ銘柄群ではない。日によって監視対象銘柄が変わるため、
  銘柄選定バイアス（後から見て相場が良かった銘柄が多く混ざる可能性）が
  ゼロとは言えない。
- エントリー閾値・利確幅・損切り幅（0%/1.5%/1.0%）は依然として暫定の
  共通値で最適化していない。

## 手法選択レイヤーの土台（2026-09-14）

「その日の地合い・需給・気配値から期待値の高い手法を選ぶ」という方針を受け、
scripts/morning_review.py に regime_at_decision（investor_regime.jsonのレジーム名）と
supply_phase_at_decision（credit_supply.jsonの信用需給フェーズ）を、朝の決定時点の
情報としてタグ付けする処理を追加した。scripts/method_selector.py を新規作成し、
このタグ×side（LONG/SHORT）ごとに勝率・平均R・PFを集計する土台を作った（実データで
動作確認済み、現状はタグ付き記録0件のため全区分「データ収集中」）。

**正直な制約**：
- タグは今日追加したばかりで、過去169件の記録には付いていない。したがって
  当面は意味のある集計結果が出ない。数週間〜1か月分のデータが貯まってから
  初めて「レジームX×需給改善の日はREBOUND LONGが強い」といった判断ができる。
- **気配値（寄り前の板の状態）はまだどこにも記録されていない**。これは
  マーケットスピード2 RSS（ユーザーのPC側のExcel）にしかない情報で、
  GitHub Actions側からは取得できない。気配値を条件に含めるには、ユーザーが
  毎朝手動で何らかの形（簡単な入力フォーム等）で記録する仕組みを別途作る必要が
  あり、今回は未着手。当面はレジーム・信用需給の2軸のみで手法選択を検討する。

## キオクシア大幅GD（-8.33%）当日の値動き検証（2026-09-14）

ユーザーのTradingViewチャート（45秒足、OR15/VWAP/EMA等表示）を受け、kioxia_5m_calendar.jsonの
current_1m（1分足実データ、09:12〜15:25）とtechnical_levelsを実際に集計して検証した
（推測ではなく実データ集計）。paper_trade_history.jsonは100銘柄LONGスキャン専用で
キオクシア個別は記録対象外のため、本検証は手動チャート判断の参考用。

**検証済み事実**：
- ギャップ: `current_gap_pct` -8.33%、寄り付き(session_open) 49,500円（前日終値換算で約54,000円台）
- 安値 48,730円（OR15安値と一致、09:12-09:17頃）→ VWAP初奪還 09:19（価格49,230円 vs VWAP 49,210円）
- OR15高値(50,100円)上抜け確定 09:42。押し目は09:45の50,390円→10:20の49,480円まで、
  ただし終始VWAP（当時49,665-49,673円）を割らずに保持→10:25頃に切り返し再確認
- 高値51,140円（14:38）、終値50,590〜50,860円。ギャップは高値到達時点でも埋まっていない
  （前日終値推定54,000円に対し約-5%）→ gap_studies「GD 5%以上」バケットのgap_fill_rate 0%
  （n=5、小標本）と整合

**2つのエントリー方法をSTATUS.mdのREBOUND LONGルール通りに比較（実データでR倍数試算）**：

| 方法 | エントリー | 損切り | リスク | 目標1(R) | 目標2(R) |
|---|---|---|---|---|---|
| A: VWAP初奪還直後の追随（09:19-09:22） | 49,230円 | OR15安値48,730円割れ | 約500円(1.0%) | 50,100円(R≈1.7) | 51,140円(R≈3.8) |
| B: 押し目（VWAP保持）確認後（10:24-10:25） | 49,870円 | 49,400円（VWAP・安値49,480円の下） | 約470円(0.9%) | 50,650円(R≈1.7) | 51,140円(R≈2.7) |

Aは値幅を大きく取れるが、09:45→10:20に評価益が一時大きく削られる区間を保有し続ける必要があった
（振り落とされずに耐える難度が高い）。Bはdocs/AI_SHARED_SHEET.mdおよび既存のvwap_entry_analysis.py
の結果（VWAP未満押し目エントリー: 勝率61.9%・PF4.28 ＞ VWAP以上追随エントリー: 勝率18.2%・PF0.08）
と方向性が完全に一致する実例。「VWAPを完全に割らずに保持したまま切り返す押し目」は、
「VWAPを一度割ってから奪還するパターン」よりさらに質が高い可能性がある（今回は前者）。

**正直な制約**：
- n=1（本日1件）の事後検証であり、統計的結論ではない。既存のvwap_entry_analysis.pyの知見と
  方向が一致する追加の実例として位置づける。
- 今日のGDの具体的な材料（決算・格下げ・需給要因等）はkioxia_catalysts.jsonに該当日の記載がなく
  特定できていない。ChatGPT側でのマクロ・個別材料確認を推奨（役割分担どおり）。
- current_1mは09:12開始（寄り付き〜09:11の気配・歩み値はyfinance分足に反映されない）。
  厳密な寄り付き数秒間の値動きは未検証。

**次に検討すべきこと**：「VWAPを割らずに保持する押し目」を「一度VWAP割れしてから奪還する押し目」と
区別して検知する簡易ロジック（例: 押し目中の最安値がVWAPを下回ったか否かのフラグ）を
vwap_entry_analysis.py等に追加し、複数サンプルで質の違いを検証する余地がある（未着手）。

### 【訂正】2026-09-14（ChatGPT指摘・AI_SHARED_SHEET.md C-002 / Claude再検証・C-003）

上記の検証に**事実誤認**があったため訂正する。ChatGPTからdocs/AI_SHARED_SHEET.mdのC-002で指摘を受け、
自分が生成した1分足VWAP系列を再計算して確認した。

- **誤り**：「押し目は…ただし終始VWAPを割らずに保持」と書いたが誤り。実際は**10:16〜10:23の8分間、
  価格（49,580円→49,480円）がVWAP（約49,672〜49,665円）を明確に下回っていた**。10:24（49,760円）に
  VWAP上へ再奪還している。生データで再計算し確認済み。
- **連動する誤り**：上表の方法B（エントリー49,870円・10:25、当時VWAP約49,668円）は**エントリー時点でVWAPより上**であり、
  vwap_entry_analysis.pyの定義に従えば「押し目型（entry<VWAP）」ではなく「**追随型（entry≥VWAP）**」に分類される。
  したがって「方法Bは既存知見（押し目型が優位）を裏付ける実例」という結論は**逆方向であり撤回する**。
  むしろ既存の枠組みでは追随型（勝率18.2%・PF0.08、過去データ）に該当する可能性がある。
- **OR15安値の訂正**：現行のcurrent_1mは09:12開始のため、09:00〜09:15の完全な範囲をこのデータ単独では
  独立検証できていない。`technical_levels.or15_low`（48,730円）はkioxia_calendar.py側の計算結果を
  引用したものであり、本検証の1分足データだけで裏付けたわけではない。
- **GDの材料（新規に確認・独自にWebSearchで検証）**：Reuters等の報道により、Anthropic CEOのDario Amodei氏らが
  「AI開発ペースの減速」を呼びかけたことを受け、9/14朝方にアジア市場でAI関連銘柄が軒並み急落
  （SK Hynix -5.3%、Samsung Electronics -3.7%、SoftBankは一時-13%等）。半導体・メモリセクター全体の
  地合い要因であり、キオクシア固有のIR材料ではないことを確認した。前回「材料不明」としていた点を訂正する。
- 上表のエントリー/損切り/目標の**価格そのもの**（49,230円、48,730円、49,870円等）は生データと整合しており訂正不要。
  誤っていたのは「VWAPを割らなかった」という前提と、そこから導いた解釈のみ。
- 生データ上、10:22（価格49,600円、VWAP約49,665円）はエントリー時点でVWAP未満の「押し目型」に該当しうるが、
  これは終日の値動きを知った上で後から選んだ一点であり、確定足・出来高等の生きた判断基準での
  再現性は未検証。後知恵バイアスを避けるため、これを新たな「優位な実例」として提示しない。

**結論**：n=1の当日検証から「押し目型が優位」という既存知見を追加で裏付けることはできなかった
（むしろ今回のB案は反証寄りの実例）。既存知見（vwap_entry_analysis.py、n=43+程度）自体を疑う根拠にもならない
（n=1のため）。今後の当日検証では、エントリー候補ごとに「entry時点のVWAPとの上下関係」を機械的に
判定してから分類する（目視・文章での判定はしない）。

## Issue #17対応：キオクシアRSS日時表示・東証/夜間PTS区別の修正（2026-09-14）

ChatGPT起票のGitHub Issue #17（キオクシアRSS: 東証と夜間PTSの区別・日時表示の修正）に対応。
作業前にPC上で実際に稼働中のキット（`...\デイトレ\MarketSpeed II RSS\MS2_RSS_100_Stocks_Live_Kit\`、
`old/`配下ではない方）とリポジトリ版`ms2_live/Kioxia_RSS_Live_Watcher.ps1`を比較し、内容が完全一致
（改行コードの違いのみ）であることを確認してから着手。

**作業前に検知した状況**：着手時、実際にExcelとKioxia_RSS_Live_Watcher.ps1が稼働中だった
（Excel起動18:36、Watcherプロセス18:38、現在時刻19:2x台）。ユーザーに確認の上、Watcherプロセスへ
CTRL_C_EVENTを送って正常終了（finally節で`RSS接続!B16`が「停止」になることを実機で確認）させてから、
ライブファイルのバックアップを取得（`old/Kioxia_MS2_RSS_Live_Signals_backup_20260914_192619.xlsx`）した上で着手した。

**実施した修正**（`ms2_live/Kioxia_RSS_Live_Watcher.ps1`、PC実使用版の両方に同一内容で反映済み）：
1. `計算!B18`（最終更新）・`B19`（足時刻）、`DASHBOARD!B22`・`D22`にNumberFormat（`mm/dd hh:mm[:ss]`）を
   明示設定。これまでExcelがVBA/COM経由の文字列を日付シリアル値として自動変換し、表示セルの書式が
   「G/標準」のままだったため生の数値（例: 46279.808...）で表示されていたのが原因（実機で再現・確認済み）。
2. `DASHBOARD!A24/B24`に「データ鮮度」セルを新設。Excel自身の`NOW()`で`計算!B18`との差分を計算する
   数式にしたため、Watcherスクリプトが異常終了してもExcel側だけで鮮度警告が機能する。
3. 東証RSS（`RssMarket("285A.T",...)`）は夜間PTS(JNX)の価格を返さないため、$inNightPts/$inDayPts
   判定を追加し、状態表示（`計算!B25`→`DASHBOARD!F22`）に「東証終了・JNX夜間PTS時間帯（本ツールは
   PTS価格未対応）」等を表示するようにした。**PTS価格そのものは取得・表示しない**
   （Issue完了条件「確認できなければPTS値を作らず未対応/未取得にする」に従った）。
   PTS時間帯の目安（夜間16:30〜翌6:00、デイタイム8:20〜9:00）は一般公開情報に基づく推定であり、
   楽天証券側の実際の受付時間・MS2 RSSでのJNX価格取得可否は未検証（下記参照）。
4. 売買サイン（`$buy`/`$short`）・音声通知は従来どおり`$inSession`（東証通常立会時間）のみで判定し、
   夜間PTS判定の追加が誤発動に影響しないことをコードレビューで確認（ロジックを完全に分離）。

**実機検証済み**：
- 上記1〜2の修正はライブファイルにも直接反映し、Excel上で表示を確認（シリアル値の再発なし、
  データ鮮度が時間経過で正しく「古いデータ(N分前)」に変化することを確認）
- 現在時刻（19時台、夜間PTS時間帯と想定される時間帯）に`RssMarket("285A.T","現在値")`が値を
  返さない（RSS取得エラー）ことを実機で確認。プレーンな「285A.T」ではPTS価格が来ていない可能性を
  裏付ける根拠として記録
- 両ファイル（リポジトリ版・PC実使用版）の構文エラーなし、内容完全一致を確認

**未確認（実機で確認できておらず、完了扱いにしない）**：
- Watcher再起動後、新しい状態文言（「東証終了・JNX夜間PTS時間帯…」）が実際に画面へ反映されるかは
  **未確認**。安全のためユーザー承認なしにWatcherを再起動していない
- 東証ザラバ中の動作（本対応中は取引時間外だったため検証不可）
- JNX完全終了時間帯（6:00〜8:20想定）の表示切り替え
- 楽天証券における実際のJNX夜間/デイタイムPTS受付時間（一般公開情報からの推定に留まる）
- MS2 RSSの公式仕様書は未参照。RssMarket以外の関数・市場コードでJNX/PTS価格を取得できるかは未検証
- 配布キット（Excelブック本体）への反映：ライブブックには実際の取引ログ（検証ログシート）が
  含まれるため、公開リポジトリには追加していない（機微データの公開回避を優先）。配布用テンプレート
  として何を公開すべきかはユーザー判断待ち

## 気配値比率のログ記録を追加（2026-09-14・未確認）

ユーザーから「その日の地合い・気配値比率に合わせてもっと柔軟に対応する必要がある。それがこの
システムを作ろうとした目的で、結果（プロフィット）がすべて」という方針を受けた。既存の
`scripts/method_selector.py`・regime/supply_phaseタグ付け（「手法選択レイヤーの土台」参照）を
拡張する前提として、STATUS.mdで既知の課題だった「気配値（寄り前の板の状態）が未記録」を解消する
第一歩として、`Kioxia_RSS_Live_Watcher.ps1`にOVER/UNDER気配数量のCSVログ記録を追加した
（リポジトリ版・PC実使用版の両方に同一内容で反映、構文エラーなし・内容一致を確認済み）。

**実装内容**：
- 同じフォルダーに`気配値ログ.csv`（日付,時刻,OVER気配数量,UNDER気配数量,UNDER比率,
  最良売気配値,最良買気配値,状態）を1分に1回追記
- 現在値（`$price`）がまだ無い寄り前の時間帯でも記録できるよう、価格nullガードより前の位置に実装
  （従来のロジックは価格nullなら気配も含めて即continueしていたため、寄り前は一切記録されていなかった）
- このCSVはローカル専用。`.gitignore`に`ms2_live/*.csv`を追加し、公開リポジトリには絶対にアップロードしない

**実機検証（今回できたこと）**：CSV書き込みロジックをスクリプト外で単体テストし、正しい値・
0除算時の空文字・日本語エンコードが機能することを確認（実際のExcel/RSS接続は使用していない）

**未確認（ユーザーの意向で本日はWatcherを再起動していないため）**：
- 実際のExcel/RSS接続を通じて`気配値ログ.csv`が正しく生成・追記されるか
- 寄り前（8:20〜9:00頃）の気配数量が実際にRssMarketから取得できるか（東証の気配提示自体は寄り前から
  行われる想定だが、MS2 RSSでの提供有無・タイミングは未検証）
- 明日以降の朝、実際に寄り前からWatcherを起動して確認する必要がある

**次に検討すべきこと**：数日〜数週間分のログが貯まったら、日付をキーに
investor_regime.json（地合い）・credit_supply.json（信用需給）・気配値ログ.csv（気配値比率）を
結合し、method_selector.pyの集計軸に気配値比率を追加する。ただしCLAUDE.mdの方針どおり、
生の板情報そのものを公開リポジトリに転記しない（集計済み・匿名化された特徴量のみ検討）。

## 全自動化への一歩：起動チェーン統合とJNX(夜間PTS)価格取得の発見（2026-09-14）

ユーザー方針「最終的にはMS2起動のみ自分でログインし、あとは全て自動化したい」を受けて対応。

**1. 自動起動チェーンへKioxia Watcherを追加**：`AUTO_START_MS2_100.ps1`（リポジトリ版・PC実使用版）に
`Kioxia_RSS_Live_Watcher.ps1`の自動起動を追加（MS2ログイン→Excel RSS接続確認後、100銘柄収集器と並行して
自動起動。多重起動防止チェックあり）。確認したところ**平日8時の自動起動タスク自体がWindowsに未登録**
だった（`INSTALL_WEEKDAY_AUTO_START.ps1`は用意済みだが一度も実行された形跡なし、`auto_start.log`も存在せず）ため、
ユーザー承認を得てタスクスケジューラーへ登録した（タスク名「AI Cockpit MS2 Live」、状態Ready、実行実績はまだ0）。

**2. Issue #17の訂正：JNX（夜間PTS）価格は実は取得できていた**：既存の`MS2_RSS_100_Collector.ps1`に
`RssMarket("285A.JNX", ...)`という仕組みが既に実装されており（`KIOXIA_JNX`シート、`kioxia_jnx_pts.csv`等、
Issue #17着手時に見落としていた）、**実際に稼働中のライブデータで動作を確認した**：

| 確認時刻 | PTS現在値 | 前日比 | 気配(買/売) |
|---|---|---|---|
| 20:01:27 | 51,000円 | +0.81% | 50,913円 / 51,039円 |
| 20:07:29 | 50,574円 | -0.03% | 50,510円 / 50,679円 |

Issue #17で「PTS価格は未対応」としたのは、東証ティッカー（`285A.T`）でしかRssMarketを試しておらず、
`.JNX`ティッカーの存在に気づいていなかったための不十分な判断だった。実データで動作確認できたため、
`Kioxia_RSS_Live_Watcher.ps1`のDASHBOARDに「JNX参考」欄（現在値・前日比・気配・時刻）を追加した。
**売買サイン（`$buy`/`$short`/`$inSession`）には一切使っていない**（コードレビューで完全分離を確認済み）。
実機（ライブExcel）に直接反映し、上表の実データで表示確認済み。

**正直な制約**：
- 東証RSS(`285A.T`)が現在値エラーの状態での状態文言（`計算!B25`）も、PTS価格が表示専用であることを
  明記するよう更新したが、これはコード上の変更であり、Watcher再起動後の表示は未確認
  （本日はユーザー指示によりWatcherを起動していない）
- `KIOXIA_JNX`シートの`.JNX`ティッカーは285A以外の99銘柄分も定義されているが、今回検証したのはKioxia(285A)のみ
- `.JNX`のデータ品質（スプレッド・出来高の妥当性、MS2公式仕様との整合）は今回確認した2時点のスナップショットの
  限りでは異常値なし、という以上のことは言えない。継続的な信頼性検証は未着手
- タスクスケジューラー登録は今回が初回のため、実際に来週の平日8時に正常動作するかは**未確認**
  （次回平日朝に実行ログ`auto_start.log`と気配値ログ.csvの生成を確認する必要がある）

## Excel COM書き込みエラー（C-005/C-006）の根本原因特定・解消（2026-09-14夜）

ChatGPTが共有シートC-006で提示した診断方針（例外の型・HResult・InnerExceptionを区別して記録する）に
従って対応し、**コード自体のバグではなく、デバッグ中の運用ミスが原因だったことを特定した**。

**最優先で対応した安全対策**：DASHBOARD!B5（売買サイン表示）の数式を、Excel自身の`NOW()`と
`計算!B18`（最終更新時刻）を比較する形に変更し、1分以上更新が止まっていれば「⚠古い・使用不可」を
表示するようにした（`ms2_live/Kioxia_RSS_Live_Watcher.ps1`起動時に上書き設定）。計算!B2（サイン本体）は
書き込みバッチの最初のセルで、その書き込みが失敗すればバッチ全体が中断され計算!B18も更新されない
（コードレビューで確認済み）ため、この鮮度判定はPowerShell側の生死に関わらずExcel単体で機能する。
これによりCOM書き込みが失敗し続けても、画面に古い「買いサイン」等が有効な現在の情報として
表示され続けることはない。

**診断ログの追加**：`Set-CellValue`関数の失敗時に、例外の型・HResult（16進）・InnerException・
書き込もうとした値の型・対象セル番地を`com_error_diag.csv`（ローカル専用）へ記録するようにした。

**この診断ログにより判明した事実**：失敗時のHResultは一貫して`0x80004002`（**E_NOINTERFACE**）。
これは.NET側の型変換ミスではなく、COMオブジェクトが要求されたインターフェースを持たないという
COM特有のエラーで、長時間起動し続けているCOMサーバー（Excel）に対して、プロセスの異常終了等で
壊れたプロキシ参照が蓄積した場合に典型的に発生する。今夜のデバッグ中に`Stop-Process -Force`で
Watcherプロセスを何度も強制終了していたことが原因と推定し、**Excelを完全に終了→再起動したところ、
同一のコードがエラーなく動作することを実機で確認した**（15秒間の稼働で診断ログにエラー0件）。

**結論**：`計算!B2`等への書き込みが失敗する問題は、コードの欠陥ではなく、長時間起動し続けたExcelの
COM状態劣化が原因だった可能性が高い。リトライ機構(`Set-CellValue`)と鮮度ゲート(DASHBOARD!B5)は
念のため実装したまま残す（本来のサイン鮮度保証としても有効なため）。

**正直な制約（未確認のまま）**：
- 今回の検証は市場時間外（23時台）の15秒間のみ。ChatGPTが要求した「寄り前・ザラバでのRSS鮮度・
  書込成功率」の実機確認はまだ行っていない
- 「Excelの長時間起動＋強制終了の繰り返し」が本当の原因と断定するには、意図的な再現実験
  （同じ操作を繰り返してエラーを再現させる）までは行っていない。今回は1回の再起動で解消したという
  観察に基づく推定
- 通常の運用（自動起動チェーンで毎朝Excelを新規に開く）であれば、今回のような長時間起動＋
  頻繁な強制終了は発生しないはずだが、それでも本番相当の環境での確認は未実施

## 自動起動のMS2待機ロジックを修正（2026-09-14夜）

ユーザーから「マーケットスピードIIの起動はパスキー認証等で時間がかかり、MS2が完全に起動する前に
Excelファイルを開くとRSSタブ自体が出ずRSS接続できない」という指摘を受けた。`AUTO_START_MS2_100.ps1`
（リポジトリ版・PC実使用版）は、これまでMS2起動後に**固定15秒待つだけ**でExcelを開いており、
パスキー認証が長引く日には確実にRSS接続に失敗する設計だった。

**修正内容**：固定15秒待機を廃止し、MS2起動後に音声・画面で「ログイン完了後にEnterキーを押してください」
と案内し、`Read-Host`でユーザーがEnterキーを押すまで待つように変更した。ログイン完了の自動検知は
パスキー認証の性質上不可能なため、ユーザー自身の確定操作に委ねる設計とした（タスクスケジューラー
登録時に`-WindowStyle Normal`を指定済みのため、この対話的な待機は実際に機能するはず）。

**正直な制約**：
- 構文検証・PC実使用版との内容一致は確認済みだが、**実際にMS2起動→Enterキー入力→Excel起動という
  流れが実機で機能するかは未確認**（次回の起動時に確認が必要）
- Excel起動後の「準備完了」判定は、依然としてワークブックが開いたことしか見ておらず、RSS接続が
  実際に成功したかどうかまでは確認していない（別の既知の課題、今回は未対応）

## 実機再テストの結果：COM書き込みエラーは未解決（2026-09-14深夜）

上記「Excel COM書き込みエラーの根本原因特定・解消」で「Excel再起動で解消した」と報告したが、
**実機での再テストで再現性がないことが判明した**。訂正する。

**今回確認した追加のバグ（これは確定・修正済み）**：`AUTO_START_MS2_100.ps1`の`Start-Process
-ArgumentList`に、スペースを含むパス（このフォルダー自体がそう）を配列の1要素としてそのまま渡すと
引用符が付かず、子プロセスが`-File`引数を正しく受け取れず起動直後に痕跡なく終了することを実機で
再現・確認した。単一の文字列でパスを明示的にダブルクォートする形に修正し、プロセスが残ることを
確認済み。戦略音声・信頼度監査・Kioxia Watcherの3箇所すべてに同じ問題があったため修正した。

**未解決（訂正）**：上記の修正後、Excelを完全に閉じてから新規に開き直し、Kioxia Watcherを起動した
ところ、**今回は私が一切追加の診断接続をしていない状態でも、起動から約20秒でCOM書き込みエラー
（InvalidCastException、HResult 0x80004002）が88件発生した**。前回「1回の再起動で解消した」のは
再現性のない偶然だった可能性が高い。新しい仮説（未検証）：`Start-Process`で外部からExcelを開き、
複数の別プロセス（Watcher・収集器等）がそれぞれ`GetActiveObject`で後から同じインスタンスに
接続するという構成自体が不安定性の原因かもしれない（Excel自身が`New-Object -ComObject
Excel.Application`で自らインスタンスを生成・所有する場合の方が安定する可能性）。

**重要**：最優先だった安全対策（DASHBOARD!B5の鮮度ゲート）は両方のテストで正しく機能した。
古いデータのまま「有効なサイン」として表示され続けることはない。ただし裏を返すと、**実際に
機能する売買サイン表示自体がまだ実現できていない**。ユーザーへは「修正・実機確認が完了する
まで売買サインを使わない」よう引き続き伝えている。

**次回への申し送り**：
- 「Excel自身がCOMインスタンスを生成・所有する」設計（外部からStart-Processで開くのではなく、
  Kioxia_RSS_Live_Watcher.ps1自身が`New-Object -ComObject Excel.Application`し`Workbooks.Open`する）
  に統一できないか検証する余地がある（今回は未着手）
- 複数プロセスが同じExcelインスタンスに同時にCOM接続する構成そのものを見直す必要があるかもしれない
- ChatGPTへ共有し、AI_SHARED_SHEET.md C-008として追加検証を依頼した

## 真の根本原因を特定・全問題解消（2026-09-14深夜・最終）

ユーザーから「簡単にあきらめるな」との指示を受け継続調査した結果、**複数の独立したバグが重なっていた**
ことが判明し、すべて修正・実機確認した。

**発見した根本原因（すべて実機で再現・特定・修正済み）**：

1. **`$excel.CalculateFull()` / `$excel.Calculate()` がワークブック全体を再計算していた**：
   Microsoft公式仕様どおり、`Application.Calculate`系は開いている全ブックを再計算する。
   今日の自動化統合により同一ワークブックに100銘柄収集器の「100銘柄RSS」シート（100行×31列の
   RssMarket数式）と「KIOXIA_JNX」シート（100行×15列）が同居するようになり、Kioxia Watcherが
   2秒ごとに行うフル再計算が非常に重くなっていた。**これがCPU使用率0%でのハング・COM書き込み
   エラーの主因だった**。`$rss.Calculate()`（RSS接続シート単体の再計算）に変更して解消。
2. **DASHBOARD!A5:H9が結合セルだった**：安全ゲート数式を`B5`（結合セルの非アンカー）に設定していたため、
   Excelが書き込みを無音で無視し（エラーにもならない）、**最優先の安全対策が一晩中一度も有効になって
   いなかった**。実際の表示はアンカーセルの`A5`から来ることを実機で特定し、`A5`へ設定するよう修正。
3. **`Start-Process -ArgumentList`にスペース入りパスを配列で渡すと引用符が付かない**：
   子プロセスが`-File`引数を正しく受け取れず起動直後に痕跡なく終了する。単一文字列で
   ダブルクォートする形に修正（`AUTO_START_MS2_100.ps1`の3箇所）。
4. **JNX参考価格(`$ptsPrice`、生のdouble型)をCOM経由でそのまま書き込むとキャスト例外**：
   `[string]`変換してから書き込むよう修正。
5. **MS2はパスキー認証等でログイン完了までの時間が読めない**：固定待機を廃止し、
   ユーザーがEnterキーを押すまで待つ方式に変更（`AUTO_START_MS2_100.ps1`）。

**実機で確認した最終結果**：修正後、Kioxia Watcherを起動し**75秒間連続でCOM書き込みエラー0件**、
`計算!B18`（最終更新）が継続的に最新時刻を示し、`DASHBOARD!A5`（安全ゲート付きサイン表示）が
正しい数式・正しい値（「待機」）を表示し、`DASHBOARD!B25`（JNX参考価格）も実際に価格が更新される
（50,100円→50,200円）ことを確認した。プロセスも安定して稼働し続けている。

**正直な制約**：
- 今回の検証は市場時間外（23時台）の75秒間のみ。寄り前・ザラバでの実市場時間での動作確認は
  まだ行っていない（次回の平日朝、実際の自動起動チェーンで確認する必要がある）
- 売買サイン（買い/空売り）が実際に発動する場面（`$buy`/`$short`が真になる状況）は今夜は
  発生しなかったため、その経路自体は未検証（シグナルロジック自体は今回変更していない）
- リトライ機構（Set-CellValue/Set-CellFormula）は今後も安全網として残す。今回の根本原因（重い
  全体再計算）が解消された後もエラーが完全に出ないとは限らないため

## 表示改善＋完全自動化チェーンをコールドスタートから実機確認（2026-09-15未明）

ユーザー実機のスクリーンショットで「現在値」表示が東証の15:30引け値のまま凍結され、下のJNX参考価格
（実際に動いている）と乖離して見えて紛らわしい、との指摘を受けた。ラベルを「現在値(東証)」と
明示するだけでは不十分だったため、**表示自体をその時点で意味のある値に自動切替**するよう修正した
（東証立会中は東証値、それ以外はJNX夜間/デイタイムPTS参考値）。売買判定（`$buy`/`$short`、
OR15/VWAP/EMA計算）は引き続き東証データ（`$price`）のみを使用し、この表示切替とは完全に独立
していることをコードレビューで確認済み。実機で「現在値(JNX夜間PTS参考) / 49,989円」のように
正しく切り替わることを確認した。

続けて、**タスクスケジューラー経由のコールドスタートから完全自動化チェーンを実機確認**した：
Excel・MarketSpeed II・Watcher・収集器を全て終了 → タスク`AI Cockpit MS2 Live`を手動実行
（8時の自動発火と同一処理）→ MS2起動 → ユーザーがEnterキーでログイン完了を確定 → Excel自動起動 →
戦略音声・Kioxia Watcher・100銘柄収集器が自動で順次起動、という流れが実際に動作し、
Kioxia Watcherの診断ログはエラー0件だった。

**ユーザー方針（2026-09-15未明）**：「システムは何とか動くようになったから、これからが本番。
MS2のRSS機能をふんだんに活用して、売買できるシステム構築をしよう」という次フェーズへの
移行方針が示された。ChatGPTへ共有し意見を求めた（AI_SHARED_SHEET.md C-010）。

**正直な制約**：
- コールドスタートの自動化チェーンは深夜（市場時間外）の1回のみ確認。寄り前・ザラバでの
  同じ流れの確認はまだ
- 「売買できるシステム構築」の具体的な範囲・優先順位はまだユーザーと合意できていない
  （MS2 RSSの板・歩み値・信用残高等のどの機能をどう活用するかは次回の相談事項）

## ChatGPT C-011指摘の安全ゲート停止耐性バグを修正（2026-09-15未明）

ChatGPTが共有シートC-011で提示した懸念（Microsoft公式のExcel再計算仕様上、`NOW()`はvolatile関数
でも「再計算がトリガーされた時」にしか再評価されず、時間経過そのものでは再評価されない）を
隔離環境（本番Excelとは別の`New-Object -ComObject Excel.Application`インスタンス）で実機検証した。

**懸念は事実だった**：DASHBOARD!A5の安全ゲート数式に新鮮な`計算!B18`をセットして「買いサイン」を
表示させた状態で、以後`計算!B18`を一切更新せず（main watcherが完全にクラッシュした状態を模擬）、
75秒間まったく再計算トリガーを与えずに放置したところ、A5は「古い・使用不可」へ切り替わるべき
はずが**「買いサイン」のまま凍結**していた。これまで安全ゲートが機能していたのは、Watcher自身が
2秒ごとに`$rss.Calculate()`を呼んでいたことに偶然支えられていたためで、Watcherが完全に停止する
（エラーで落ちる、強制終了される等）とこの前提が崩れることを実機で確認した。

**対策**：Watcher本体とは完全に独立した最小限の「心拍」プロセス`Kioxia_Safety_Heartbeat.ps1`を
新規作成した。これは5秒ごとにDASHBOARDシートを再計算するだけの単機能プロセスで、売買判定・
表示内容には一切関与しない。Watcherがどんな理由で落ちても、この心拍プロセスが生きている限り
安全ゲートのNOW()判定は正しく更新され続ける設計。`AUTO_START_MS2_100.ps1`（自動起動チェーン）に
組み込み、Watcherと並行して自動起動するようにした。

**修正実装で2回の実機テスト失敗を経て真因を特定した（正直な経緯）**：
1. 1回目の実装：「どのシートでも良い、Worksheet.Calculate()を1回呼べばブック内のvolatile関数が
   全て再評価される」という誤った前提で計算シート（Worksheets.Item(1)）を再計算していた。
   実機テストで20秒経ってもDASHBOARD!A5が更新されないことを確認し失敗と判明。
2. 2回目の実装：安全ゲート数式があるDASHBOARDシートを明示的に指定するよう修正したが、それでも
   改善しなかった。原因を調査した結果、`GetActiveObject("Excel.Application")`は、その時点で
   Excelインスタンスが複数（本番＋過去の検証で残った孤立インスタンス等）存在すると**どのインスタンス
   へ接続するか不定**であることが判明した。心拍プロセスが目的のワークブックを持たない別インスタンス
   へ接続してしまい、ワークブック名が一致せず何も再計算されていなかった。
3. **最終修正（実機検証済み）**：`[Runtime.InteropServices.Marshal]::BindToMoniker($bookPath)`で
   ファイルパスのモニカーから目的のワークブックへ直接バインドする方式に変更した。これはExcel
   インスタンスの数に関わらず目的のワークブックだけを確実に指す。**Excelインスタンスが3つ同時に
   存在する状態で実機テストを行い**、心拍プロセス開始から20秒以内にDASHBOARD!A5が正しく
   「買いサイン」→「古い・使用不可」へ切り替わることを確認した。

**実機検証の方法**：すべて本番Excel（`Kioxia_MS2_RSS_Live_Signals.xlsx`、ライブ稼働中）とは別の
隔離テスト用ワークブック・別プロセスで実施し、本番データ・本番の売買サイン表示には一切触れていない。

**正直な制約**：
- 今回の検証は市場時間外の隔離テストのみ。実際の自動起動チェーンで心拍プロセスと本番Watcherが
  同時に正常稼働するかは次回の実機コールドスタートで確認が必要
- DASHBOARDシートの重さ（数式数）が将来増えた場合、5秒間隔の再計算負荷が問題になる可能性は
  未検証（現状は軽量なシートであることを前提にしている）

## ChatGPT C-013指摘への対応：4パターン受入試験＋失敗の可視化＋既知の限界の明記（2026-09-15未明）

C-012の心拍プロセスに対し、ChatGPTから共有シートC-013（ユーザー経由）で受入試験の要求と
重要な指摘があった。**「WatcherとHeartbeatが両方同時に停止した場合、Excel数式だけでは
表示を自動的に無効化できない」という指摘は正しい**。この限界がある限り「常時fail-safe」とは
謳わないことにする。

**実施した対応**：

1. **心拍プロセスの失敗を可視化**（`ms2_live/Kioxia_Safety_Heartbeat.ps1`）：従来は全例外を
   黙殺していたため「監視されていない状態」が「正常稼働」と見分けられなかった。連続失敗回数を
   `heartbeat_diag.csv`（ローカル専用、`.gitignore`の`ms2_live/*.csv`で既に保護対象）へ記録し、
   3回連続失敗（約15秒）で音声警告（5分間隔で繰り返し）を鳴らすようにした。成功時も1分に1回
   「正常」ログを残すことで、ログの更新自体が止まっていれば「プロセスごと落ちた」ことも
   区別できるようにした。

2. **4パターンの隔離受入試験を実施**（すべて本番Excel非接触、独立した検証用ワークブックのみ使用）：

| パターン | 条件 | 結果 | 判定 |
|---|---|---|---|
| 1. Watcher停止・Heartbeat稼働 | 新鮮なB18のまま75秒放置（心拍なし）→心拍開始→20秒後に確認 | 75秒時点で「買いサイン」のまま凍結（バグ再現）→心拍開始20秒後に正しく「古い・使用不可」へ切替 | 期待どおり（実物の`Kioxia_Safety_Heartbeat.ps1`ファイルで検証） |
| 2. Heartbeat停止・Watcher稼働(模擬) | 2秒ごとにB18更新+DASHBOARD再計算を20秒継続 | 常に「買いサイン」（データが実際に新鮮なため正しい） | 期待どおり |
| 3. 両方停止 | 新鮮なB18のまま80秒放置（トリガー皆無） | 「買いサイン」のまま凍結（しきい値1分を超えているのに更新されない） | **C-013指摘どおりの既知の限界を実機再現** |
| 4. HeartbeatのBindToMoniker連続失敗 | 存在しないファイルパスを指定 | 診断ログに連続失敗1〜4回目が例外詳細付きで正しく記録された（黙殺されない） | 期待どおり |

パターン1回目の試験では待機時間の設計ミス（鮮度しきい値1分を超える前に判定していた）により
見かけ上失敗したが、これはテストスクリプトの不備であり心拍プロセス自体の不具合ではなかった。
待機時間を修正して再検証し、上表の結果を得た（正直な経緯として記録）。

3. **既知の限界を明記**：パターン3で実機確認した通り、WatcherとHeartbeatが両方停止した場合、
   Excelの数式だけでは表示（DASHBOARD!A5等）を自動的に無効化する手段が存在しない
   （ExcelのNOW()は再計算トリガーなしには自走しないという仕様上の制約のため）。この残存リスクへの
   対策（心拍プロセス自体の外部監視、またはExcel VBAのApplication.OnTimeタイマーをワークブック
   自体に組み込んでPowerShellプロセスの生死に依存しない設計にする等）は未着手。「常時fail-safe」
   という表現は使わない。

**正直な制約（未着手・未確認のまま）**：
- 実際の自動起動チェーンでの実機コールドスタート確認（次回の課題、C-013項目①）
- 心拍プロセス自体の外部監視の仕組み（心拍が落ちたことを心拍以外の何かが検知する設計）は未着手
- 売買サイン発動経路（`$buy`/`$short`）自体の紙トレード検証（C-013項目⑤）は未着手

## 実機コールドスタートでHeartbeat統合後の新規バグ2件を発見・修正（2026-09-15未明）

C-013①（実際のMS2ログイン後にWatcher/収集器/Heartbeatが同時に正しく起動するかの実機確認）に
対応するため、ユーザーの承認を得て稼働中のExcel・MarketSpeed II・Watcher・収集器をすべて安全に
終了し（Excel COM経由でWorkbooks.Close→Quit、ゾンビ化したプロセスはStop-Process -Forceで完全終了。
過去のメモリどおり、Quit()だけでは不十分でプロセスをkillしないとMS2 RSSアドインが次回正しく
読み込まれない）、タスクスケジューラー`AI Cockpit MS2 Live`経由で実機コールドスタートを実行した。

**発見した新規バグ2件（いずれもHeartbeatを自動起動チェーンへ統合した今回初めて露呈）**：

1. **Watcherの起動直後クラッシュ（RPC_E_CALL_REJECTED）**：ユーザーがMS2へログインしEnterキーを
   押した直後、`Kioxia_RSS_Live_Watcher.ps1`が`$excel.Visible = $true`（Excel接続直後の最初の
   COM呼び出し）で`COMException: 呼び出し先が呼び出しを拒否しました (HRESULT 0x80010001
   RPC_E_CALL_REJECTED)`により即座に落ちた。Excel起動直後・RSS接続処理中はCOM呼び出し自体が
   拒否される（いわゆる「サーバービジー」）ことがあり、この呼び出しにはリトライ処理が
   なかったため`$ErrorActionPreference = "Stop"`によりスクリプト全体が終了していた。
   `Set-CellValue`/`Set-CellFormula`と同様の考え方で、起動時の一連のCOM呼び出し
   （`Visible`設定、`DisplayAlerts`設定、ワークブック検索/オープン、ワークシート取得）を
   包括的にリトライする`Invoke-ComRetry`ヘルパーを追加し、実機で再現・解消を確認した。

2. **Heartbeatが起動直後に痕跡なくクラッシュ**：自動起動チェーンのログには
   「safety heartbeat started」と出るが、実際にはプロセスが即座に落ちており、診断ログ
   （heartbeat_diag.csv）すら作成されていなかった。2つの独立した原因があった：
   - **原因A（エンコーディング）**：`Kioxia_Safety_Heartbeat.ps1`はこれまでWriteツールで
     作成したためBOMなしUTF-8で保存されていた。Windows PowerShell 5.1はBOMがないと
     UTF-8と認識できず、ファイル内の日本語文字列を誤って解釈しパースエラーで即終了していた。
     これまでの隔離テストは「読み込んでBOM付きで書き直した一時コピー」を使っていたため、
     この問題を一度も検知できていなかった（テスト方法自体の盲点）。BOM付きUTF-8で
     再保存して解消。
   - **原因B（OneDrive同期パスの扱い）**：本番ワークブックはOneDriveで同期されたフォルダーに
     あるため、ExcelはROT(Running Object Table)へローカルファイルパスではなく
     `https://d.docs.live.net/...`形式のOneDriveクラウドURLで登録することを実機で確認した。
     これまでのC-012/C-015での検証はOneDrive非同期の一時テストフォルダーで行っていたため、
     この差異を検知できていなかった。`Marshal.BindToMoniker(文字列)`はクラウドURL形式の
     モニカーを解釈できず失敗し続けたため（`MkParseDisplayName`による文字列再解釈が
     OneDriveの独自アイテムモニカーに対応していないと考えられる）、文字列を介さず
     ROTから見つけたモニカーを`IRunningObjectTable.GetObject()`で直接取得する方式に変更。
     本番のOneDrive同期ワークブックに対して実機で9分以上連続でエラーなく動作することを確認した。

**実機で最終確認した状態**：Watcher・Heartbeat・100銘柄収集器がすべて同時稼働し、
`計算!B18`（最終更新）が継続的に更新され、`DASHBOARD!A5`（安全ゲート）が正しい値（「待機」）を
表示し、`RSS接続!B4`（東証参考値）にも実際の価格が入っていることを確認した。

**正直な教訓**：これまでの隔離テストは「本物に近いが本物ではない」環境（OneDrive非同期の
一時フォルダー、BOM付きで書き直したコピー）で行っていたため、環境差異に起因する2つの
実運用バグを見逃していた。ユーザーからの「まずはシステムの安定起動優先」という指示のもと
実際にコールドスタートしたことで初めて発見できた。今後、本番相当の検証は可能な限り
本番と同一のフォルダー構成・ファイル（OneDrive同期含む）で行う。

**正直な制約（未確認のまま）**：
- 今回の確認も市場時間外（深夜）。寄り前・ザラバでの動作は次回市場日まで持ち越し
- 売買サイン発動経路（`$buy`/`$short`）自体はまだ検証していない（C-013⑤、次回の課題）
- Heartbeatの新しいROT列挙方式は、Excelインスタンスが多数（3つ以上）かつOneDrive同期時に
  高頻度（5秒間隔）で列挙し続けた場合のCPU負荷は今回未計測

## 次フェーズ開始：状況適応型サイン構築（2026-09-15未明）

ユーザー方針（2026-09-15未明）：「RSSを活用してその日の状況に合わせた売買サインの構築を
ChatGPTと意見交換しながら、今日9/15のザラバで実践。最終的な売買判断は本人が行うので、
Claude/ChatGPTの思考過程も共有しながら進める」という次フェーズ開始の指示を受けた。

**正直な制約（今夜のうちに明確化）**：気配値ログ.csv（板のOVER/UNDER気配数量記録、
今夜0時台に実装・開始）は現時点で夜間PTS時間帯のデータしかなく、全行で数値が完全に
凍結している（板が動いていないため）。東証ザラバ中の実データは今日9/15が初回。
したがって「地合い・気配値比率に応じて閾値を自動調整する」検証済みモデルを今日の
寄りまでに用意することはできない。既存の`$buy`/`$short`条件（VWAP・5分EMA9/20・OR15・
出来高比）は今日から変更しない方針とし、まずは表示専用の参考情報追加から着手する。

ChatGPTへ方針と制約を共有し、優先的に観測すべき指標・検証に必要なサンプル数の目安等の
意見を求めた（docs/AI_SHARED_SHEET.md C-017）。

## MS2 RSS共通データ基盤（2026-09-15）

- `MS2_Common_Engine.ps1` を追加し、全対象銘柄を5分類で共通判定する枠組みを実装。
- 100銘柄収集器の各銘柄へ、1分/5分足、共通スコア、取得網羅率、安全ゲート、売建可能数量を追加。
- 自動発注は固定OFF。注文関数は未実装。注文一覧・信用余力・保証金率が未確認の間は必ず売買禁止。
- 既存TOP5、キオクシア専用画面、15:25固定、Claude共有シート運用は削除・置換していない。
- PowerShell構文解析と共通判定テストは合格。MS2/Excel実機での売建可能数量、10本板、口座一覧、ザラバ負荷は未確認。

## Codex統合分（MS2共通判定基盤）の実機反映と致命的バグの修正（2026-09-15未明）

ChatGPT/Codexが直接mainへコミットした`MS2_Common_Engine.ps1`（全銘柄共通のTREND LONG/SHORT/
REBOUND/RANGE/NO TRADE判定とfail-closed安全ゲート、共有シートC-019）について、ユーザーから
明示的に「自分が依頼したものなので反映してほしい」との指示を受け、実機反映作業を行った。

**発見した問題1（軽微）**：新規ファイル2件（`MS2_Common_Engine.ps1`、`tests/test_ms2_common_engine.ps1`）
がBOMなしUTF-8で保存されており、Windows PowerShell 5.1がパースできずテストが即エラーになった
（これまで複数回遭遇した既知のパターン）。BOM付きで再保存し解消。

**発見した問題2（致命的・実機で発見）**：`MS2_Common_Engine.ps1`冒頭の`Set-StrictMode -Version Latest`が、
`MS2_RSS_100_Collector.ps1`からdot-source（`. (Join-Path ...)`）で読み込まれることで
**呼び出し元スクリプト全体のスコープへ漏れ出し続ける**（PowerShellのdot-sourceの仕様）。
1300行超の既存収集器コードはStrictMode非対応で、Excel COMオブジェクトとの相性問題
（Windows PowerShell 5.1の既知の不具合）により、100銘柄分のJNX RSS数式設定ループの途中で
「null 値の式ではメソッドを呼び出せません」という実行時エラーが発生し、**収集器プロセス全体が
起動直後にクラッシュする**ことを実機で確認した（最初の反映試行では見かけ上動いているように
見えたが、これは古いプロセスが生きていたタイミングと重なっただけで、実際には新コードは
一度も正常起動していなかった。コンソール出力をファイルへリダイレクトして再実行し初めて
発見できた）。`Set-StrictMode`の行を削除して解消。修正後、実機で「100銘柄のRSS監視を
開始しました」まで到達し、80秒以上安定稼働、`live_ms2.json`に`schema_version`・
`capabilities`・`common_decision`・`account_gate`等の新フィールドが正しく出力されることを確認した。

**実機で確認した安全動作**：`account_gate.order_allowed = false`、`status = "売買禁止"`。
注文一覧・信用余力・保証金率・売建可能数量がいずれも未確認（実際の口座API接続は未実装）のため、
自動発注OFF（既定）と合わせて常に売買禁止状態になることを確認した——fail-closed設計どおり。

**正直な制約（未確認のまま）**：
- `売建可能数量`のRSS数式（`RssMarginInfo("285A.T","売建可能数量")`）は追加されたが、実機の
  JSON出力では値が取得できていない（null）。深夜のため信用情報がMS2側で提供されていない
  可能性があるが、関数名・フィールド名がMS2 RSSの実仕様と一致しているかは未検証。次回の
  実市場時間で再確認が必要
- 「時間帯統計の更新を保留: "TryParse"のオーバーロードで、引数の数が"2"であるものが
  見つかりません」という軽微な警告が出ているが、既存のtry/catchで正しく捕捉されており
  致命的ではない。原因は未調査（Codex統合とは無関係の可能性が高い、既存コードの潜在バグ）
- 口座・注文・建玉・含み損益・保証金率の実データ接続は未実装（`$null`固定）。実際にこれらを
  接続するまで、共通判定・安全ゲートは「計算はできるが常に売買禁止」の状態が継続する
- 今回の検証は深夜・市場時間外のみ。寄り前・ザラバでの実データでの共通判定精度・RSS負荷は未確認

## 決算発表予定銘柄ウォッチをAIコクピットへ新規実装（2026-09-15）

ユーザー依頼「明日以降の当日注目を浴びそうな決算銘柄も、ChatGPTと方向を検討しながら
AIコクピットに実装してほしい」への対応。ユーザーは「方向予想は確率的な方向性スコア
（上昇/下落/中立）として実装、ただし検証データが貯まるまでは試運転中表示を必須」と
明示的に選択した。

**実装内容**：
- `scripts/upcoming_earnings.py`（新規）：JPX公式「翌営業日決算発表予定会社」
  （認証不要・毎営業日17時頃更新）を取得し、既存の100銘柄ウォッチリストとの
  重複チェック、直近5日/20日の株価モメンタムに基づく仮説的な「方向レーン」を
  付与して`upcoming_earnings.json`を出力する。`margin_caution_check.py`と同じ
  「素のHTML→0件ならPlaywright再取得」パターンを踏襲。
- `.github/workflows/upcoming-earnings.yml`（新規）：JPXの17時頃更新後を狙い、
  平日17:30 JSTに実行する独立ワークフロー（`board-buzz.yml`と同じ構成）。
- `data/earnings_predictions_log.json`（新規・自動生成）：日々の予想を記録し、
  target_dateが過ぎたら実際の翌日リターンを取得して的中判定する予測ログ。
  `reliability_report.py`と同じ考え方で、resolved_nが閾値（暫定n=20）未満の間は
  「試運転・検証中」と表示し、的中率を確立した手法として扱わない。
- `scripts/update.py`：AIコクピット（index.html）へ新セクション「決算発表予定・
  注目候補（試運転）」を追加。クライアント側JSで`upcoming_earnings.json`を
  取得・表示する既存パターン（event_calendar.json等と同じ方式）。

**正直な制約（実装時点で明確化）**：
- JPXのこのページは「翌営業日」分のみを提供し、2日以上先の確定リストは無料の
  公式ソースからは取得できない。「明日以降」という依頼のうち確実に満たせるのは
  実質「翌営業日」分のみ
- 対象は3月期・9月期決算会社の本決算・四半期決算のみ。全上場企業を網羅しない。
  9月は決算閑散期のため「該当銘柄なし」の日も多い（JPX公式ページを直接確認した
  時点でも翌営業日分はゼロだった）
- 「方向レーン」は決算内容そのものの予測では断じてない。直近の株価モメンタムのみに
  基づく仮説で、増減益・ガイダンス・市場コンセンサスは一切考慮していない
- **この開発環境にはPythonが導入されておらず、スクリプトをローカルで実行・検証
  できていない**（既存のkioxia_calendar.py等、Yahoo Finance/JPXへ接続する他の
  スクリプトも同様の制約があったことがSTATUS.mdの過去記載から確認できる、
  この案件固有の問題ではない）。構文は目視でのレビューのみ。GitHub Actions上の
  実行結果とログで初めて動作確認ができる
- ChatGPTとの「決算内容を踏まえた定性的な検討」は、このスクリプト単体では
  完結しない。共有シート経由で個別銘柄が出た際に都度依頼する運用とする

## 決算発表予定銘柄ウォッチ：Codexが実機検証し公開ページ表示まで確認（2026-09-15）

C-023でCodexへ引き継いだ`.github/workflows/upcoming-earnings.yml`の追加・実行検証が完了した。
`workflow`スコープの制約はCodex側の資格情報では問題にならず、GitHub連携API経由でmainへ
追加された（gh OAuthのスコープ自体は変更していないとのこと）。

**Codexが実施・発見した内容（docs/C-023_ACTIONS_VERIFICATION.md参照）**：
- cronの曜日指定バグを発見・修正（`0-4`＝日〜木だったのを`1-5`＝月〜金へ）。当方の実装ミス
- 表のないHTMLで`pandas.read_html`が`html5lib`不足エラーになり、Playwrightフォールバックへ
  到達できない不具合を発見し、`flavor="lxml"`明示で修正
- 表抽出0件を無条件に成功（＝該当銘柄なし）として扱っていた点を修正。JPX公式の「該当なし」
  明示文言がある場合のみ0件を正当な結果とし、それ以外（HTML構造変化等）は失敗扱いにして
  誤って「本日はゼロ件」と静かに見せない設計に強化
- 各段階の構造化診断ログ・取得前後HTML・実行ログ・出力JSONをActions artifactとして14日間保存
- 回帰テスト`tests/test_upcoming_earnings.py`を追加（数値/英字混在コードの解析、公式0件成功
  ケース、想定外HTML＝失敗ケース、通信例外＝失敗ケースをカバー）
- workflow_dispatchで3回実行し、最終実行でJPX HTTP 200→pandas解析→Playwrightフォールバック
  →再解析→JPX公式の「翌営業日の開示予定会社はございません」を確認→JSON出力→mainへpushまで
  エラーなく完走したことをログ・commit・artifactで確認済み

**Claudeが追加確認した内容**：実際に公開ページ（https://infoyuusuke-afk.github.io/trade-cockpit/ ）を
開き、新セクション「決算発表予定・注目候補（試運転）」が実データで正しく表示されていることを
目視確認した（`試運転・検証中（サンプル不足）（resolved_n=0）`、空データ時の案内文言も含め
設計どおり）。

**正直な制約（Codexの記録どおり、まだ残っている）**：
- 実データで検証できたのは「該当銘柄0件」の経路のみ。実際に銘柄がある日の表構成・
  モメンタム取得・予想ログの追加/解決・方向精度は未検証
- 13:56の手動実行はJPXの17時頃更新より前だったため、当日17:30の定時起動そのものは
  まだ実績がない
- 金曜・祝日前後のtarget_date（翌暦日と翌営業日のズレ）の扱いは未実装
- resolved_n=0のため、方向レーンの的中率は依然まったく検証されていない

## MS2 RSSの未活用機能を実データで検証・RssTickList実装（2026-09-15）

ユーザー（ChatGPT経由の提案）から「MS2 RSSは単なる株価取得用ではなく、10本板・歩み値数量・
信用建玉・注文発注までAIコクピットの中核にできる。それを実装できていないのでは」という
指摘を受けた。実装済み/未実装を正直に棚卸しした：

| 機能 | MS2 RSSで可能か（公式仕様で確認済み） | 実装状況 |
|---|---|---|
| 現在値・1分足〜60分足・OR5/OR15・VWAP・EMA・出来高加速・成行数量・OVER/UNDER・信用残 | ◎ | ✅ 実装済み |
| 10本板（RssBoard） | ◎（楽天公式ヘルプで確認） | ❌ 未実装 |
| 歩み値の約定数量（RssTickList） | ◎（楽天公式ヘルプで確認） | ❌ 未実装だった→**今回実装** |
| 売建可能数量（RssMarginInfo） | ◎ | △ Codexが追加したが値未取得（要検証） |
| 建玉・含み損益・注文状況・信用余力・保証金率 | ◎ | ❌ 未実装（account_gateは常にnull固定） |
| 自動発注（RssOrder） | ◎（楽天公式ヘルプで確認） | ❌ 未実装（発注関数はコード上に一切存在しない） |

ユーザーは「RssTickListで本当の歩み値（数量付き）を実装」を優先として選択した。

**実装内容**：`Kioxia_RSS_Live_Watcher.ps1`に`=RssTickList(,"285A.T",300)`を追加（RSS接続!A530、
既存の1分足/5分足チャート範囲と衝突しない位置に実機確認の上で配置）。ヘッダー行＋直近300件の
個別ティック（時刻・出来高・約定値）を取得できることを実機で確認した。これまでの「歩み1〜4」
（RssMarket、価格・時刻のみで数量なし）では、出来高急増が「本当の大口注文」か「小口の積み重ね」
かを区別できなかったが、`RssTickList`は実際の約定出来高そのものを持つため、この区別が初めて
可能になった。実機で、大引け15:30:00の408,700株という単一ティックを、直前の100〜800株の
小口ティックと明確に区別して検知できることを確認した。

新規関数：`Read-TickList`（300行のティック表を解析）、`Get-TickFootprints`（直近300ティックの
出来高中央値の8倍以上を大口候補として抽出）、`Get-TickFlowBias`（各ティックの約定値を最良気配と
比較し出来高加重した買い/売り優勢度、-100〜+100）。DASHBOARD!A34（大口ティック検知）・A35
（歩み値偏り）に表示。既存の1分足ベースの推定（A28・A32）は削除せず併記し、比較できるようにした。
いずれも「参考・未検証」と明記し、`$buy`/`$short`・音声通知には一切使っていない。

実機でWatcherを再起動し、起動エラー0件、両セルとも実データで正しく表示されることを確認した。

**正直な制約**：
- 出来高中央値の8倍という閾値は完全に暫定・未検証。今日1日の的中率等の検証はまだ行っていない
- 大口ティックであっても「機関投資家の1件の注文」と断定はできない（同一ティックに複数の
  小口注文が同時約定した可能性は理論上ゼロではない）
- 10本板（RssBoard）・建玉/含み損益/注文状況/信用余力/保証金率・自動発注（RssOrder）は
  今回対応しておらず、引き続き未実装のまま

## 10本板（RssBoard→RssMarket番号付きフィールド）を実機実装（2026-09-15）

上記の棚卸しで「未実装」だった10本板について、`RssBoard`という関数を実際にExcelで
`=RssBoard(,"285A.T")`と試したところ`#NAME?`エラーとなり、**この関数はMS2 RSSに存在しない**
ことを実機で確認した（ユーザー/ChatGPT経由の提案にあった関数名は誤り）。楽天公式ヘルプを
再確認し、正しい方法は`RssMarket("285A.T","最良売気配値N")`／`"最良売気配数量N"`／
`"最良買気配値N"`／`"最良買気配数量N"`（N=1〜10）であると判明、これに切り替えて実装した。

**実装内容**：RSS接続シート行870〜879に、N=1〜10それぞれの売気配値・売気配数量・買気配値・
買気配数量の計40セルを設定（`Invoke-ComRetry`でラップ）。`Read-BoardDepth`関数で10段を配列化し、
買い/売りの厚み比率（買計株数÷全体）と、最大枚数のレベル（"壁"）を買い/売りそれぞれ検出、
DASHBOARD!A37に「10本板(実データ)」として表示。実機で買2本目に16,100株の実在する買い壁を
検知し、買い厚み比率64.8%（買計25,800株/売計14,000株）と正しく算出されることを確認した。
「参考・未検証」の対象データであり、`$buy`/`$short`・音声通知には一切使っていない。

**実機投入時に発生した問題と対応**：
- 起動セットアップで40個超のセル書式を新規に設定するようになったことで、既存のHeartbeat・
  100銘柄Collectorと同時にExcelへCOMアクセスする際の競合が激化し、起動シーケンス中に
  `0x80010001`（RPC_E_CALL_REJECTED、サーバービジー）で3回連続クラッシュした。
  毎回クラッシュする行が異なり（RssTickList式の行→既存の「データ鮮度」セル→既存の5分足式の行）、
  特定の1行のバグではなく起動時間が伸びたことによる全体的なCOM競合と判断した。
- 対応は段階的に実施：(1)板セットアップループを`Set-CellFormula`から`Invoke-ComRetry`
  （8回・500ms間隔）に変更→単独では解消せず、(2)`Set-CellValue`/`Set-CellFormula`の
  デフォルトリトライを4回/150ms→20回/300msに引き上げ→これも単独では解消せず
  （別の行で再度クラッシュ）、(3)**最終的に効いた対応**：Watcher・Heartbeat・Collectorの
  3プロセスを全停止し、約5秒待ってからWatcherのみを単独起動（他プロセスとの競合なし）→
  0エラーで正常起動を確認、その後Heartbeat・Collectorを起動。3プロセスとも正常稼働を確認した。
- ※コミットメッセージ作成時、テスト前に「実機で解消を確認した」と誤って書きかけたことに
  気付き、実機確認前は「未検証」と明記するよう修正してから実際にテストした。

**未解決の既知の問題（正直に記録）**：3プロセスを上記の手順で再起動し、Watcherの起動ログも
成功を報告していたにもかかわらず、その約20〜40秒後の診断チェックで、RSS接続!A530
（RssTickListの数式）が完全に空になっていることを発見した（Excelプロセスは1つのみ稼働している
ことを確認済みで、複数インスタンスの取り違えではない）。根本原因は特定できていない。
診断スクリプトでA530の数式を手動で再設定したところ即座に成功し、その後15秒待って
A34・A35（RssTickList依存セル）がWatcherの実行中メインループを通じて正しく実データで
再表示されることを確認した（回避策としては機能しているが、根本原因未解明のため、
今後Watcherを再起動した際に同じ現象が無警告で再発する可能性がある）。

## IR急騰PTS検知：対象範囲の作り替え＋TDnetの重大バグ2件を修正（2026-09-15）

ユーザーから「PTSの目的は引け後の決算結果・IRで急騰した銘柄の確認ができるようにすること」と
明確な方針指示を受けた。夜間PTS期待TOP5・IR急騰PTS TOP5とも売買代金1,000万円の壁でほぼ
全銘柄が「候補なし」になっていた件（前セクション参照）とは別に、IR急騰PTSには**構造的な
対象範囲の欠陥**があることが判明した：TDnet開示企業を、あらかじめ固定された100銘柄
ウォッチリストの中にあるかどうかでしか照合しておらず、東証全体約3,800社のうち100銘柄という
リストに、その日開示した企業がたまたま含まれる確率は低い。結果、`ir_pts_snapshots.csv`は
今日1日を通して1行も記録されていなかった（ヘッダーのみ）。

ユーザーは「開示企業を動的に追加（推奨）」を選択。対応として`ms2_live/MS2_RSS_100_Collector.ps1`に
新規シート`IR_DYNAMIC_PTS`を追加し、TDnet更新（120秒毎）のたびに、引け後(15:30以降)・
好材料判定（スコア>0・非ブロック）・固定100銘柄リスト外の開示があれば、その銘柄専用の
RssMarket式（TSE現在値＋JNX気配・出来高等）をその場で追加する方式にした（既定上限30銘柄、
起動時の一括追加ではなく開示1件ごとの小さな追加のみ）。取得したデータは固定100銘柄と
同じ計算式でPTSスコア化し、`pts_top5`・`ir_pts_top5`双方の判定対象に統合した。

**検証中に、この対象範囲問題とは別に、既存のTDnet取得ロジック自体に2件の重大バグを発見・修正した**：
1. **文字コード誤判定**：`Invoke-WebRequest`の`.Content`（自動デコード）を使用していたが、
   TDnetのページはHTTPヘッダーに文字コード指定がなく`<meta>`タグ頼みのため、Windows
   PowerShell 5.1が文字コードを誤判定し、日本語タイトルが実機で文字化けすることを確認した
   （生バイトは正しくUTF-8であることも確認済み）。結果、「上方修正」「増配」等のキーワード
   一致判定が終日サイレントに失敗し、全開示が強制的にスコア0（無視）になっていた。
   `RawContentStream`から生バイトを取得し、明示的にUTF-8としてデコードする方式に修正。
2. **コード抽出の誤り**：既存コードは行内の全セル結合テキストから正規表現で4桁コードを
   汎用検索していたが、TDnet実ページのコード欄は「4桁コード＋検査数字1桁」（例:
   82810→本当は8281）等の5文字形式で、この検索方式では一致せず、代わりにタイトル内の
   西暦（例:「2026年」）を誤ってコードとして拾っていた。結果、今日の全開示（実際は約166件）が
   `code=2026`または`code=2027`という偽のキーでほぼ全て潰れ、29件（実際は165件前後）にまで
   減っていた。TDnetの固定列順（時刻・コード・会社名・表題）を直接読む方式に書き替え、
   コード欄の先頭4文字（検査数字を除く）を正しいコードとして抽出するよう修正した。

**実機検証結果**：修正後の再起動で、TDnet取得件数が29件→165件（実際の当日全開示約166件と
ほぼ一致）に急増したことを確認。IR急騰PTS TOP5に本日初めて候補が2件表示され、いずれも
信越化学工業（4063、固定100銘柄リスト外）の実材料——「創立100周年記念配当の実施及び配当予想の
修正」（増配・スコア25）と「コミットメント型自己株式取得における事後調整」（自社株買い・
スコア25）——を、実際のPTS上昇（+1.6%、売買代金2,024万円）と正しく結びつけて検知した。

**実機投入時のトラブル**：この修正の反映直後、Collectorが起動処理中にクラッシュした
（`$jnxSheet`がnullで既存のJNXシート設定ループが失敗、`null 値の式ではメソッドを呼び出せません`）。
起動時のCOM呼び出しが今回の追加分だけわずかに増え、Watcher・Heartbeatとの同時アクセス競合
（本日前半の10本板実装時と同じ既知のパターン）が再発したと判断。3プロセス全停止→Collector
単独起動→Watcher・Heartbeatを順次再起動、という同日前半で有効だった手順で解消し、3プロセスとも
安定稼働を確認した。

## キオクシアタブの一本化：Excel WatcherのデータをHTMLへミラー配信（2026-09-15）

ユーザーから「Excelファイルとhtmlの2本パッケージで中身がバラバラ、Excelは裏で動かして
PC上ではHTMLだけ見ればよい形にしたい」という指摘を受けた。実態は、`Kioxia_RSS_Live_Watcher.ps1`
がExcelのDASHBOARDシートへ書く指標（10本板・実ティック大口検知・実ティックベース歩み値偏り・
気配地合い等）と、公開コクピットのキオクシアタブが`MS2_RSS_100_Collector.ps1`の簡易版
キオクシア指標（UNDER比率・歩み値偏り等、別の計算式）を表示しており、**同じ名前の指標が
別々のコードで別の数字を出す、実質2つの独立したキオクシアエンジン**になっていたことが判明した。

**対応**：`Kioxia_RSS_Live_Watcher.ps1`に、DASHBOARDへ書き込む内容と同じ生の値を
`kioxia_watcher_live.json`として出力し、ローカルHTTP（127.0.0.1:28581、既存の100銘柄収集器の
28580と同じ方式）で配信する機能を追加した。公開コクピットのキオクシアタブに「EXCEL WATCHER・
2秒監視」パネルを新設し、このJSONを直接表示する。既存のCollector由来のパネル（簡易指標）は
削除せず残し、Watcher側パネルを追加する形にした（両方を1つの計算に統合するのはリスクが
大きいと判断し、今夜は見送り）。

実装中、$biasや板の集計変数が条件分岐内でしか代入されておらず前回ループの値が残る
stale変数バグ（100銘柄収集器で見つけたのと同種）を発見・修正した。

**実機検証**：Excel・MS2は再ログインせず、Watcherプロセスのみ再起動（Excel COMは接続維持）。
http://127.0.0.1:28581/kioxia_watcher_live.json が全項目実データで応答することを確認
（tick_count=300等）。GitHub Actions経由で公開ページへ反映し、ブラウザで新パネルの
レイアウト・フォールバック表示（自宅PC外からは127.0.0.1に届かないため「未接続」表示になる、
これは正常）を確認した。

**再発した既知の問題**：Watcher再起動後、RSS接続!A530（RssTickList数式）が再び空になる現象
（前回セクション参照）が今夜2回目発生した。今回は「Collector稼働中にWatcherだけ再起動」という
パターンで発生しており、同一ワークブックを複数プロセスが同時にRSS更新している際に起きやすい
可能性があるが未確証。手動再設定で復旧したが、根本原因は依然不明。

## 製品ビジョン・全体要件が確定（2026-09-15・ユーザー方針）

ユーザーから、AIコクピットの大前提となる完成形が明確に示された。要点：①デイトレTOP5＋本日の
実現損益、②オーバーナイトTOP5（引け買い・翌朝寄り売り等）、③スイングTOP5(1週間)、④長期TOP5
(1か月)、⑤決算連動の期待値銘柄（ロング/ショート）、⑥要人発言等の注意アラート、⑦地合い
（トランプ相場・金利高等）に応じた手法の自動切替（裏側で動く）、⑧Appleアプリ的にシンプルで
かっこいいデザイン、⑨将来的な販売・トレード日記自動配信による収益化（将来構想）。

自動発注については「最終目標は完全自動、現時点ではサイン生成＋ワンタップ確定（最終判断は人）」
と確定した。CLAUDE.mdの「AIが人の確認なしに自律発注しない」制約とは矛盾しない（人が最終確認
ボタンを押す前提のため）。

詳細な現状棚卸しと、ChatGPTへ相談したい5つの戦略論点（地合い適応ロジック、決算×期待値銘柄の
ロング/ショート判定基準、4階層の優先順位、期待値の統一定義、注意アラートの対象基準）は
[docs/AI_SHARED_SHEET.md「製品ビジョン・全体要件」セクション](docs/AI_SHARED_SHEET.md)に記載した。
実装はChatGPTの戦略回答を待って着手する。ワンタップ確定・実発注機能は安全設計が特に重要なため、
着手前に改めてユーザーの承認を得る。

## 決算カレンダーの2系統統合（2026-09-15）

ユーザーから「決算カレンダーの2系統統合」を指示された。以前から`scripts/upcoming_earnings.py`
（Claude製）と`scripts/earnings_calendar.py`（Codex製）が並行して存在し、公開コクピットにも
別々のパネルとして表示されていた問題（C-025で指摘・放置していた）への対応。

調べた結果、単純な重複ではなく役割が異なっていた：

- `earnings_calendar.py`：JPX予定表（今日から60日先まで）＋TDnet開示のキーワード分類
  （上方修正/下方修正/増配/減配等）で「好材料あり／警戒材料あり」を判定。カバレッジが広いが、
  方向性の的中率を検証する仕組みがなかった
- `upcoming_earnings.py`：JPX翌営業日分のみでカバレッジは狭いが、株価モメンタムに基づく
  方向レーンを出し、その的中率を継続的に自己検証（resolved_n・hit_rate_pct）していた

ユーザーに3つの統合案（①earnings_calendar.pyへ一本化しupcoming_earnings.pyは廃止、
②データは両方残し表示だけ一つに、③モメンタム・検証ループをearnings_calendar.pyへ完全統合）を
提示し、③を選択された。

**実装**：`momentum_lean`・`load_log`・`save_log`・`resolve_pending`を`earnings_calendar.py`へ
ロジック変更なしで移植し、新規に`apply_momentum`関数を追加。カレンダー中「直近で最も近い次の
決算発表日」に該当する銘柄だけにモメンタムレーンを付与する（60日分全銘柄に付けると計算コストが
大きく、決算直前の参考情報という原意からも外れるため）。予測ログ`data/earnings_predictions_log.json`
は同じパスを使い続け、過去の検証履歴をそのまま引き継いでいる。

**副次的に直ったバグ**：旧`upcoming_earnings.py`は「翌営業日」を`今日+1暦日`で計算していたため、
週末を挟むとJPXが実際に示す翌営業日（例: 金曜なら月曜）とズレる可能性があった。統合版は
JPX日程表が持つ銘柄ごとの実際の発表日を直接参照するため、このズレは発生しない。

**表示側**：`earnings-calendar.js`（Codexの決算カレンダータブ）に、モメンタムレーンと
的中率検証状況の表示を追加。`update.py`から重複していた旧パネル（試運転・決算発表予定候補）と
そのJSフェッチを削除した。

**削除したファイル**：`scripts/upcoming_earnings.py`、`tests/test_upcoming_earnings.py`、
`.github/workflows/upcoming-earnings.yml`、`upcoming_earnings.json`。

**テスト**：`tests/test_earnings_calendar.py`にログ往復・的中判定・対象日選定のテストを追加
（yfinance呼び出しはモック化、ネットワーク接続不要）。

**正直な制約**：この開発環境にPythonが導入されておらずローカル実行・構文チェックができていない
（括弧の対応関係を目視で確認したのみ）。GitHub Actions上の次回実行（`earnings-calendar.yml`が
`test_earnings_calendar.py`を実行してから本番取得する）が実質的な初検証になる。

## 決算カレンダー統合の実機初回実行：テストとデータ取得は成功、保存ステップのみ失敗（2026-09-15）

統合後の`earnings_calendar.py`をGitHub Actions上で初めて実行した（run 34962617624）。
**テスト（新規追加分含む）・JPX/TDnet取得・モメンタムレーン計算は全て成功**し、ロジック自体は
正しく動作することを確認した。しかし最後の「Save data and source status」ステップで失敗
（exit code 128、`cannot pull with rebase: You have unstaged changes`）。原因は、
`apply_momentum`が新たに`data/earnings_predictions_log.json`へ書き込むようになったにも
かかわらず、ワークフローの`git add`がこのファイルを含めていなかったこと。結果、この回の
`earnings_calendar.json`更新（モメンタムレーン・的中率検証データ含む）はmainへ反映されずに
失われた（データの誤りではなく、単なる保存漏れ）。

修正自体（`git add`に`data/earnings_predictions_log.json`を追加する1行）は特定済みで
ローカルにコミット済みだが、`.github/workflows/earnings-calendar.yml`の変更となるため
このセッションのgh CLIトークンでは`workflow`スコープ不足によりpushできない（C-023と同じ
既知の制約）。docs/AI_SHARED_SHEET.md（C-031）でCodexへ引き継いだ。反映後、初めて本番の
`earnings_calendar.json`にモメンタムレーン・的中率検証データが載ることになる。

## P0着手：共通シグナル契約の基盤を実装（2026-09-15）

ChatGPTの戦略回答（docs/C-031-GPT_STRATEGY_ROADMAP.md）のP0「保存失敗の解消、RSS鮮度・欠測・
再起動、時点固定ログ、共通期待値契約、注意アラート」のうち、地合い判定・決算判定より前に
必要な「共通の型」の部分から着手した。

**新規実装：`scripts/signal_contract.py`**（+`tests/test_signal_contract.py`）

- `DataPoint`：観測値1件（value/source_url/published_at/fetched_at）。`available_at`
  （=max(published_at, fetched_at)）を持つ
- `check_freshness`：観測値がdecision_asof時点で利用可能・鮮度内かを判定
  （ok/stale/missing/future）。`future`は「発表後データを発表前asofへ渡していないか」の
  未来情報漏洩検知（ロードマップ受入基準#2）
- `gate_no_trade`：いずれかの入力がok以外ならNO TRADE（WAIT）とする。呼び出し側は
  ブロック時にEV・勝率をゼロ補完してはならない（受入基準#1）
- `build_signal`：ロードマップ第7節の共通出力スキーマ（schema_version・policy_version・
  decision_asof・snapshot_id・horizon・side・ev_net_r・block_reasons等）でシグナル辞書を
  組み立てる。`trading_enabled`は引数として受け付けず常にFalse固定で出力する
  （CLAUDE.mdの自動発注禁止方針を、呼び出し側のミスでも破れない形で強制）
- `record_snapshot`／`read_snapshots`：意思決定の時点固定ログ。1日1ファイルのJSON Linesで
  追記保存し、後から「その時点で本当に入手可能だったデータだけ」から意思決定されたかを
  検証できるようにする（受入基準#2・#8）

このモジュール自体は戦略ロジック（地合い判定式・決算判定式）を持たない共通基盤であり、
現時点ではどの本番シグナル（$buy/$short・LIVE売買パネル等）にも接続していない。
P1で地合い判定・デイトレTOP5を実装する際の土台として使う想定。

**テスト**：`tests/test_signal_contract.py`（鮮度判定・NO TRADEゲート・schema・snapshot往復の
単体テスト、外部通信なし）。`.github/workflows/update.yml`の`python -m unittest discover -s tests`
に自動的に含まれるため、次回のUpdate Trade Cockpit実行で自動検証される。

**未対応のまま残るP0項目**：
- **保存失敗の解消**（C-031、決算カレンダーワークフローのgit add漏れ）：修正1行を特定済みだが
  ワークフローファイルのためこのセッションからpushできず、Codexへの引き継ぎ待ちが続いている
- **RSS鮮度・欠測・再起動の実運用ゲート**：上記モジュールは基盤のみで、実際にMS2 RSSの
  各スクリプトへ組み込む作業はまだ
- **注意アラート**：ロードマップ第5節の要人発言等の通知システムは未着手

## RSS鮮度ゲートを実運用スクリプトへ組み込み（2026-09-15）

P0の「RSS鮮度・欠測・再起動」に対応し、`scripts/signal_contract.py`の鮮度ゲートを
実際のシグナル生成スクリプトへ接続した。対象は`scripts/swing_signal.py`・
`scripts/long_signal.py`（スイングTOP5・長期TOP5のMVP、いずれも入力データの鮮度を
一切チェックせず使っていた）。

**実装内容**：
- `signal_contract.py`に`parse_jst_timestamp`を追加。このリポジトリのJSONで使われる
  2種類の時刻表記（ISO形式・"YYYY-MM-DD HH:MM:SS JST"形式）を解析し、"未取得"のような
  非時刻文字列は推定補完せずNoneを返す
- `swing_signal.py`：`credit_supply.json`・`investor_regime.json`の`updated_at`を
  `check_freshness`で判定（週次データのため秒単位TTLではなく10日を健全性チェックの
  目安として使用）。いずれかがstale/missingなら全銘柄`direction="WAIT"`・`score=null`
  とし、既存のスコアリングロジックを一切実行しない（古いデータでスコアを計算して
  ゼロ補完することはしない）
- `long_signal.py`：`credit_supply.json`・`correlations.json`を同様にゲート。
  `buybacks.json`は現状「未取得」の空プレースホルダー（programs=[]なら加点なしという
  既存設計）であり、実データを継続更新している別ファイルと同列に鮮度判定すると
  常時WAITになってしまうため、ゲート対象から意図的に除外した
- 両スクリプトの出力JSONに`data_quality`ブロック（各入力の鮮度判定・trade_allowed・
  block_reasons）を追加し、公開コクピット側でも判定保留の理由を確認できるようにした

**テスト**：`parse_jst_timestamp`の単体テストを`tests/test_signal_contract.py`に追加
（ISO形式・独自JST形式・"未取得"・None入力の4パターン）。

**正直な制約**：`swing_signal.py`・`long_signal.py`自体は独立ワークフロー
（`.github/workflows/swing-long-signals.yml`、平日15:40 JST実行）で動くが、
このワークフローには単体テスト実行ステップが無く、`main()`もファイルパスが
ハードコードされているため今回は単体テストを追加していない（既存のこの2ファイルも
元々無テストで、他のスクリプト群と同じ慣習）。ローカルにPython未導入のため構文は
目視確認のみで、次回のワークフロー実行が実質的な初検証になる。

## 注意アラートの基盤を実装（2026-09-15・P0完了）

P0の最後の項目「注意アラート」に対応した。ロードマップ第7節の「別カレンダーを乱立させない」
という指示どおり、新規のイベントカレンダーは作らず、既存の`scripts/event_calendar.py`
（「重要イベント」機能）を拡張する形にした。

**実装内容**：
- `time_certainty_and_utc`：イベントの`time_jst`表記から時刻確度（exact/window/
  date_only）を判定し、exactの場合のみ`scheduled_at_utc`（ISO8601、UTC）を算出する。
  「終日」はdate_only、「寄り付き」「大引け」「会合終了後」はwindow（TSEの開場・閉場や
  会合終了は目安の時間帯であり、分単位の確定時刻ではないため）とし、**時刻が不明な
  イベント（例: 日銀会合結果）に架空の正午等を割り当てることは一切しない**
  （ロードマップ第5節の明記事項）
- `notification_schedule`：Aランク（市場全体材料）はT-30分/T-5分/T時点、Bランクは
  T-5分/T時点の通知チェックポイントを生成。`scheduled_at_utc`が無い（時刻確度が
  exactでない）イベントには通知チェックポイントを生成しない
- 各イベントに`event_id`・`revision`（既定1、将来の訂正・変更検知の土台）・`tier`
  （既定"A"。このファイルの既存イベントは日銀・FOMC・CPI・雇用統計・休場・指数入替等、
  すべて公式一次情報に基づく市場全体材料のためA既定で妥当と判断）・`fetched_at`を追加
- 公開コクピットの「売買イベントカレンダー」表に「次の通知」列を追加し、tier・次の
  通知チェックポイント（またはwindow/date_onlyで時刻未確定である旨）を表示

**実装しなかった部分（正直な範囲の明記）**：
- ブラウザでの実際の音声通知・T-30/T-5/T到達時の自動発火は未実装。今回作ったのは
  「いつ通知すべきか」というデータ（スケジュール）の生成までで、実際に画面上でその
  時刻に音を鳴らす・表示を切り替える処理（ロードマップの「通知キー=event_id/revision/
  通知段階/channelで重複抑止」等）は次のステップ
- B（その他の政策関連予定発言）のイベント自体は未収集（既存のevent_calendar.pyの
  ハードコードされたイベント一覧はすべてA相当の一次情報のみで、B相当の情報源
  （中銀幹部個別の予定発言等）は接続していない）
- 銘柄固有のA（保有・発注候補の決算/業績修正、売買停止）は、`scripts/earnings_calendar.py`
  （C-030で統合済み）と本来接続すべきだが、今回は未接続のまま

**テスト**：`tests/test_event_calendar.py`を新規作成し、時刻確度判定・通知スケジュール
生成・架空時刻を作らないことの単体テストを追加（外部通信なし）。

**これでP0の5項目（保存失敗の解消・RSS鮮度ゲート・時点固定ログ・共通期待値契約・
注意アラート）のうち、保存失敗の解消（C-031、Codex引き継ぎ待ち）を除く4項目の基盤を
実装した**。次はP1（デイトレTOP5・決算発表後の監視候補・地合い共通判定・非公開売買
ログ）が控えている。

## P1着手：地合い共通判定エンジンを実装（2026-09-15）

P1の4項目（デイトレTOP5・決算発表後の監視候補・地合い共通判定・非公開売買ログ）のうち、
他の3項目が依存する「地合い共通判定」から着手した。ロードマップ第3節の仕様に沿って
`scripts/regime_policy.py`を新規実装した。

**実装内容（純粋関数・単体テスト済み）**：
- `classify_base_regime`：TOPIX代替の現在値・当日VWAP・直近3本の5分EMA20・監視母集団の
  上昇比率からUP/DOWN/RANGE/UNKNOWNを判定。必須値のいずれか欠落でUNKNOWN（推定補完しない）
- `event_lock_active`：C-030で実装したevent_calendar.jsonのtier=Aイベントの
  `scheduled_at_utc`を使い、T-10分〜T+15分の新規停止窓を判定（架空時刻は対象にしない）
- `high_vol_active`：直近60日同時間帯の5分リターン分布95%点との比較
- `apply_hysteresis`：状態機械。通常切替は2回連続成立＋最短保持15分、UNKNOWN/EVENT_LOCKは
  即時切替。ロードマップ受入基準#5（RANGE→UPが1回だけでは切替なし、2回＋保持成立で切替、
  UNKNOWNは即停止）を単体テストで直接検証
- `resolve_policy`：確定地合いごとの許可セットアップ・リスク倍率（HIGH_VOL中は0.5との
  小さい方）

**実装内容（main()のデータ取得・正直な制約）**：
- TOPIXは`watchlist_100.json`の既存の慣習と同じ代替ティッカー（1306.T、NEXT FUNDS TOPIX
  連動型上場投信）の5分足をyfinanceから取得。真の指数系列そのものではない
- ブレッドス（固定監視母集団の前日比上昇比率）は、本来PC側のMS2 RSSリアルタイム気配が
  最も正確だが、クラウド側(GitHub Actions)はPC側データに接続できないため、
  `ms2_live/watchlist_100.json`の100銘柄をyfinanceの**日次終値**で代替計算している。
  **真の日中5分足ブレッドスではない**ことを明記する
- RATE_SHOCK・POLICY_EVENTは未実装（常にFalse）。金利変化データ・政策発表分類の別途接続が必要
- 状態は`data/regime_state.json`に永続化、出力は`market_regime.json`

**テスト**：`tests/test_regime_policy.py`（25件、classify/event_lock/hysteresis/policy/
high_volの単体テスト、外部通信なし）。

**未検証のまま残る部分（正直な申告）**：この機能を定期実行する`.github/workflows/
market-regime.yml`を新規作成したが、**ワークフローファイルのためこのセッションから
pushできない**（C-023/C-031と同じ既知の制約）。したがって`main()`のyfinance実データ
取得・ファイル書き込み部分は、単体テスト（純粋関数のみ対象）を除きまだ実機検証していない。
純粋な判定ロジックは`tests/test_regime_policy.py`が次回のUpdate Trade Cockpit実行
（`python -m unittest discover -s tests`が自動収集）で検証されるが、実データ接続の
検証にはこのワークフローファイルの反映が必要。

## P1続き：決算発表後の監視候補を実装（2026-09-15）

P1の「決算発表後の監視候補」に対応した。ロードマップ第4節「別カレンダーを乱立させない」に
従い、C-030で統合済みの`earnings_calendar.py`をデータ源とし、新規`scripts/earnings_watch.py`
を実装した。出力をpre_event_watch（方向未定）／post_event_bias（材料判定はできたが売買候補
には未達）／trade_candidate（LONG/SHORT方向まで判定）の3段階に分離する。

**実装内容**：
- `classify_material_proxy`：earnings_calendar.pyの`analysis.label`（発表済み好材料あり／
  警戒材料あり／関連開示を確認／未発表・期待判断の根拠不足）をPOSITIVE/NEGATIVE/NEUTRALへ
  写像
- `classify_stage`：材料方向・反応（後述）・地合い（regime_policy.pyのmarket_regime.json）の
  3条件を順に確認し3段階に分類。SHORT方向は判定ロジックが通っても
  `short_eligibility_unconfirmed`のblock_reasonを必ず付け、実行可能扱いにしない
- `fetch_reaction_pct`：発表日前日の終値を基準にした直近終値の変化率
- 出力は`signal_contract.build_signal`で共通スキーマ化。ただしentry/stopを持たないため
  schema側の`side`は常に`"WAIT"`固定とし、方向としての有望さは`candidate_direction`という
  別フィールドに分離して記録する（執行可能なシグナルに見せない）

**正直な制約**：
- 発表内容の判定はearnings_calendar.pyの表題キーワード分類による代理指標であり、
  ロードマップが本来求める決算数値の実比較（g=(new-old)/abs(old)）ではない
- PTS反応もyfinance日次終値ベースの代理指標であり、真のPTS約定データ（PC側MS2 RSSが
  最も正確）ではない
- 証券会社の売禁・在庫・借株費用・価格規制の確認は未接続

**テスト**：`tests/test_earnings_watch.py`（16件、材料判定・3段階分類の単体テスト、外部通信なし）。

**未検証のまま残る部分**：この機能もregime_policy.py同様、実行にはワークフローが必要。
既存の`.github/workflows/market-regime.yml`（まだmainに未反映、C-036参照）に
`python scripts/earnings_watch.py`の実行ステップを追加し、1つのワークフローとして
まとめて引き継ぐことにした（regime評価の直後に実行することで、同じジョブ内で
`market_regime.json`を読める）。

## 現在の未決事項・注意点

- **Stage①（紹介前検出率）の検証は遡って行えない**：過去の株Tube公開時刻を正確に記録したログが
  今まで存在しないため、「紹介前に検出できていたか」を過去に遡って判定すると必然的に後知恵になる。
  ユーザー自身が要求した「後知恵・未来データ混入の回避」を守るには、mentions.json蓄積開始日（今日）
  以降のデータでのみプロスペクティブに検証するのが正しい。数週間〜十数週（日曜が来る回数分）は
  「検証中・データ収集中」であり、それ以前に成績を語ることはできない。
- **Stage②はキオクシア1銘柄・25日分の第一弾比較のみ完了**：上記の比較結果を参照。
  100銘柄ユニバースへの拡張、SHORT側の実売可能性データ、ティック精度への改善が残タスク。
- OR15 vs OR5（レンジ幅・天井底の判断基準）の使い分け → 上記Stage②比較でAが最良という
  結果が出たが、n=13でまだ結論を出せる段階ではない
- **A530（RssTickList数式）が再起動後に空になる現象の根本原因が未特定**：上記10本板セクション参照。
  回避策（手動再設定）はあるが恒久対応ではない
- 建玉・含み損益・注文状況・信用余力・保証金率・自動発注（RssOrder）は引き続き未実装
  （RssOrderはCLAUDE.mdの方針により自動発注には接続しない）

## 役割分担

- **Claude/Claude Code**: リポジトリの実コーディング・デバッグ・診断・バックテスト等の長時間作業
- **ChatGPT**: 相場観・戦略の壁打ち、マクロ（金利・FOMC等）の定性的な状況整理、意思決定の相談相手
- **Genspark**: 現在未課金。複数ソースのリサーチエージェントや監視ダッシュボードが必要になった時点で検討

## Kioxiaデイトレ（TradingView・45秒足）戦略の要点

- REBOUND LONG条件: OR15安値維持→VWAP奪還→EMA上向き→出来高増加→日経/半導体指数も上向き→
  板・歩み値が買い優勢、全部揃ってからのみエントリー
- REBOUND破棄→TREND SHORT: OR15安値割れ＋VWAP下＋EMA下向き＋出来高増加＋半導体全体が再び弱い場合
- 損切り: REBOUNDはVWAP再割れ or 反発起点安値割れ／SHORTはVWAP明確奪還・定着
- 利確: LONGはOR15高値/直近戻り高値でまず利確、出来高伴う突破のみ伸ばす／SHORTは直近安値で一部利確
- 持ち越し原則なし（米金利・FOMC意識のため）
