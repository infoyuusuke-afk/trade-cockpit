# MS2ローカル連携 安定稼働判定（2026-09-18）

## 判定

現状は安定稼働未確認。この文書は原因分析の共有であり、Cloudへの「安定版」引き継ぎや実機受入を意味しない。

## 確認した事実

- 100銘柄の記録は08:35:58まで寄り前データがあり、場中記録は12:30:02から15:19:51まで。09:00〜09:15の記録は無く、or15_state.jsonも存在しない。
- live_ms2.jsonの最終更新は15:19:47。有効99/100だがor15_ready=false。正式売買サインと秒スキャ候補は停止状態。
- キオクシア監視のExcel COM診断ログは26件すべて0x80010001、すべて試行1で発生。監視JSONは17:05:33まで更新していたので、少なくともこれらの単発エラーでは停止していない。
- 以前の100銘柄収集画面にMove-Itemの保存失敗が出ていた。現在の起動ファイルにはFile.Replaceと再試行を使う修正版が15:58に反映済み。構文検査と同一ファイルへの2回連続保存は成功。
- キオクシア監視の起動ファイルは17:07に更新。2秒ごとのRSSシート強制再計算を削除し、立会外の間隔を延長、終了時のローカル配信ジョブを片付ける。構文検査は成功。
- 現在はローカル配信ポート28580/28581の待受なし。停止したJSONには過去の値が残るため、利用側は更新時刻で鮮度判定が必須。
- Codexの実行環境から開いているExcelへのCOM接続はMK_E_UNAVAILABLE (0x800401E3)で失敗した。修正版の実機連続運転は未実施。

## 残る検証

1. 次の取引日の08:30までにMS2・Excel・100銘柄収集を起動し、Excel RSS接続と値更新を確認する。
2. 09:00〜09:15に有効90銘柄以上を連続取得し、09:15後に当日付or15_state.jsonとor15_ready=trueを確認する。未達なら正式サインは停止したままにする。
3. キオクシア監視を同時起動し、Excel COMエラーの頻度、30分以上のJSON更新継続、画面操作への影響を測定する。
4. 11:30、12:30、15:30をまたぎ、記録・JSON・音声が停止／再開条件どおりに動くか確認する。
5. 100銘柄収集が引けまで止まらず、保存失敗が再発しないことを確認する。実発注はOFFのまま。

## Cloud引き継ぎ条件

上記の実機結果と修正版ファイルの差分、実際の時刻付きログを添える。構文検査だけを安定稼働の証拠にしない。
